# S16_S `Slope_Form = 2` 全參數重新最佳化 —— 進度與交接（2026-08-12）

**檔案**：`strategies/research/S16_MACrossShort/S16_S_MACrossShort_v1.21.0.pla`
**狀態**：進場側 **完成**（B1 / B2 / B2b / B3 / B3b）；出場側 **未動**（B4–B13）
**下一步**：**B4 QuickStop**，330 組

---

## 0. 一頁摘要 —— 明天在筆電端從這裡開始

```
1. git pull
2. MC12 載入 S16_S_MACrossShort_v1.21.0.pla
3. 全預設跑一次，必須複現：184 筆 / 1,678,000 / PF 1.61 / 回檔 -548,400 / RoA 331.62
4. 對上了才開 B4（§5 有完整參數範圍）；對不上先停，那是環境問題不是市場問題
```

| | 值 |
|---|---|
| 起點（v1.20.0 `Slope_Form = 2` 原始格） | 748,000 ／ 128 筆 ／ PF 1.3825 |
| **現在（v1.21.0 全預設）** | **1,678,000 ／ 184 筆 ／ PF 1.61** |
| 累計 | **+124.3%** |

---

## 1. 為什麼要做這件事

v1.18.0 → v1.20.0 換了斜率公式的分子（`Fast[1]−Fast` → `(Close[1]−Close)/Close/時間`）。
進場配對比對顯示新舊母體**只重疊 28/118 筆，78% 的交易被換掉**。

所有出場參數都是在舊那 118 筆上調出來的 —— 不是「可能要重調」，是**已知失效**。
採用 Form 2 是**公式正確性**的裁示，不是績效裁示；這一點必須隨版本攜帶。

---

## 2. 已完成批次

| 批 | 內容 | 結果 | 增量 |
|---|---|---|---|
| **B1** | 進場母體 | 1,494,400 ／ 187 筆 ／ PF 1.52 | +746,400 |
| **B2** | Gap 濾網 | 1,522,000 ／ 186 筆 ／ PF 1.54 | +27,600 |
| **B2b** | Gap 三個開關補掃 | 1,522,000（**無變動**） | 0 |
| **B3** | 時段／尾盤／二次進場 | 1,678,000 ／ 184 筆 ／ PF 1.61 | +156,000 |
| **B3b** | 尾盤時間往下延伸 | 1,678,000（**確認 430 是真高原**） | 0 |

### 2.1 B1 —— ZLEMA 配對跨公式存活

**#1 落在 25/70，就是現行預設值。** v0.5 GA 在**點數式**下挑出來的配對，換了分子仍是最佳；
`ZLEMA_Fast = 25` 在前 10 名出現 6 次。**跨公式存活的參數，比只在單一公式下最佳的參數可信。**

25/70 那一欄的門檻輪廓：

| `MinSlope_Rate` | 0.040 | 0.045 | **0.050** | 0.060 | 0.100 |
|---|---|---|---|---|---|
| 淨利 | 1,088,400 | 1,163,200 | **1,494,400** | 473,200 | 478,800 |
| 筆數 | 284 | 234 | **187** | 119 | 22 |

0.040 → 0.050 是**淨利上升、筆數下降**的乾淨方向（品質改善，不是靠加碼）。

**陷阱行**：#18（18 筆、PF 4.72、RoA 628）、#23（25 筆、PF 3.28）、#44/#50（22 筆）。
2.8 筆/年，PF 是 9 個贏家算出來的雜訊，且樣本數 ≥ 100、三市況 PF、Sniper Bear PF 全部算不出來。
**照 PF 或 RoA 排序選就會掉進去。**

### 2.2 B2 / B2b —— 整個 Gap 模組值一筆交易

毛利／毛損欄位直接證明：

| | 毛利 | 毛損 |
|---|---|---|
| 187 筆 | 4,348,800 | −2,854,400 |
| 186 筆 | 4,348,800（**一分未動**） | −2,826,800 |
| 185 筆 | 4,342,800 | −2,826,800（**一分未動**） |

187 → 186 擋掉一筆**純虧損 27,600**；186 → 185 多擋的是**贏家 +6,000**，所以 `Gap_Rise = 0.0` 反而更差。

**B2b 把那兩筆分開了**：被擋的 −27,600 是**日盤**交易（所有最佳格都 `Gap_Day_On = 1`）；
被過度擋掉的 +6,000 贏家是**夜盤**交易（`Gap_Night_On = 0` 就保住它）。

`Gap_Filter_On = 0` 給 1,494,400，`Gap_Scope_Bars ≤ 2` 也給 1,494,400。**整組六個參數的全部貢獻 = 擋掉一筆日盤虧損。**

> **跳空是被公式解掉的，不是被濾網解掉的。**
> Form 2 的 ÷時間項已讓**時段第一根進場筆數 = 0**（Form 1 有 8 筆、合計 −54,800），
> 濾網要擋的對象不存在了；它唯一抓到的那筆進場在時段**第 3 根**，所以 `Gap_Scope_Bars` 必須是 3。
>
> **原規劃的 6 種以上跳空濾網（R2 收盤位置、R3 下影線、18 型 K 棒判別）不必再做。**
> 這 +27,600 值得收，但**證據等級 n = 1**，日後不可被重新詮釋成「已驗證的跳空模組」。

### 2.3 B3 —— 尾盤時間是這批的全部

| | 淨利 | 筆數 | PF |
|---|---|---|---|
| `Sess_LastBar_Block_On = 1` | **1,678,000** | 184 | 1.61 |
| `Sess_LastBar_Block_On = 0` | 1,648,800 | 185 | 1.59 |

**這個開關值 +29,200。** v1.17.0 測試時它樣本內 **0 次觸發**，當時只證明「不誤擋」；
換了母體後第一次真的作動，方向正確。**從「程式碼讀起來對」升級為「實測有貢獻」。**

**錢不只來自少做兩筆**：

| | B2b | B3 | 差 |
|---|---|---|---|
| 毛利 | 4,348,800 | 4,427,200 | **+78,400** |
| 毛損 | −2,826,800 | −2,749,200 | +77,600 |

毛利也上升 ⇒ 有交易被**換到更好的價格出場**。拆開：
- `Tail_NoEntry_From` 430 → 415：擋掉 **04:15–04:24 的 2 筆進場**，值 −48,800
- `Tail_ForceExit_Time` 440 → 430：強制平倉提前 10 分鐘，**毛利反而上升** ⇒ 04:30–04:40 這段在吐回利潤

### 2.4 B3b —— 邊界檢查，430 確認是真高原

B3 的贏家 `Tail_ForceExit_Time = 430` 卡在掃描範圍（430–500）**最低那一格**，
左側沒有格子。**邊界上的最佳值是截斷，不是高原**，依「採用值須兩側各一格同向支撐」不可採用。

B3b 往下延伸到 350：

| `Tail_ForceExit_Time` | 380 | 390 | 420 | **430** | 440（原預設） |
|---|---|---|---|---|---|
| 淨利 | 1,465,600 | 1,542,000 | 1,644,000 | **1,678,000** | 1,522,000 |
| PF | 1.53 | 1.56 | 1.60 | **1.61** | 1.54 |
| 回檔 | −567,200 | −521,600 | −543,200 | −548,400 | −548,800 |

左側 420 差 **−2.0%**、右側 440 差 **−9.3%**，都遠在 40% 規則內。**兩側支撐成立，採用。**
（430 比 420 多賺 34,000，代價是回檔多 5,200。）

`Tail_NoEntry_From` 的平帶是**單邊的**：350 / 365 / 400 / 410 / **415** 全部同值 1,678,000，
425 開始變差（186 筆、1,629,200）。取 415 —— 已證安全的**最寬鬆**值。
往下取只是在零觀測時窗上多封 5–25 分鐘，那是把「尾盤不好」外推到沒資料的地方。
（未測空缺：420，真正失效邊界落在 (415, 425]。）

---

## 3. ★ 兩個必須帶著走的教訓

### 3.1 MC12 最佳化器看不到 `TrueFalse` 型 input

**不是難掃，是完全不出現在最佳化清單裡。**
B1 和 B2 是在**沒有它們的開關**的情況下跑的 —— 報表各只有 3 個參數欄，
而不是規格書寫的 6 個和 4 個。那些開關不是「被選擇性凍結」，是**被排除**。

**v1.21.0 的修正**：17 個布林 input 改為數值 0/1，在 `SECTION 2.5` 解碼成布林鏡像變數。
下游只改識別字，仍比較 `True` / `False`。

> **為什麼不直接把 `= True` 改成 `= 1`**：re-entry 鏈有三個 `= False or` 閘門。
> 機械式取代很容易把 `= False` 寫成 `= 1`，邏輯**靜默反轉** —— 回測照跑、結果全錯。
> 包裝變數讓那三行只換名字、不動邏輯，陷阱從根上消失。

`Manual_Kill_Switch` 不轉：緊急營運開關，人在壓力下翻的東西，True/False 下拉不會弄錯、1/0 會。

### 3.2 時間類參數的 `TimeToMinutes()` 陷阱

```powerlanguage
v_Tail_Cutoff = MinutesToTime( TimeToMinutes( Tail_NoEntry_From )
                               - Tail_Signal_Lead_Bars * BarInterval );
```

`TimeToMinutes(t) = int(t/100)*60 + mod(t,100)`。**非法時鐘時間不會被拒絕，會被靜默重新詮釋：**

| 輸入 | 讀起來像 | **實際等於** |
|---|---|---|
| 365 | 03:65（不存在） | 3×60+65 = 245 分 = **04:05** |
| 380 | 03:80 | 260 分 = **04:20** |
| 395 | 03:95 | 275 分 = **04:35** |

B3b 的網格同時含這三個，**看起來均勻、實際跳躍**。

**兩條規則**：
1. 採用值必須是**合法時鐘時間**（415 = 04:15 ✓）。寫成 365 的檔案，半年後沒人推導得出來
2. 日後所有時間類參數的掃描步進，必須讓每一格都落在 **00–59 分**內（步 5／10／15 從 :00 或 :30 起跳）

另註：`Tail_Signal_Lead_Bars = 2` 會再往前扣 10 分鐘，所以 415 的**實際訊號截止是 04:05、成交截止 04:10**。

---

## 4. 惰性登記簿（實測，非推測）

以下參數經掃描，**兩種設定回傳位元相同的結果**，在現行預設下是死重。
**全部保留不刪** —— 每個都有推導、鄰近參數一動就會重新生效 —— 但這是日後簡化的第一順位。

| 參數 | 證據來源 | 為什麼是惰性的 |
|---|---|---|
| `Slope_Require_Adjacent` | B3 | Form 2 的 ÷時間項已讓時段第一根進場數歸零 |
| `Tail_Signal_Lead_Bars` | B3 | 0/1/2/3/4 **五格全部同值** |
| `ReEntry_Structure_Gate` | B3 | 第 3 次獨立確認；狀態機已蘊含 `Fast < Slow` |
| `Gap_Night_On` | B2b | 在採用的 `Gap_Rise_Min_Pct` 帶上，0 與 1 逐欄相同 |

**相對地，這些已確認是活的且承重的：**

| 參數 | 證據 |
|---|---|
| `Sess_LastBar_Block_On` | **+29,200**，首次實測作動 |
| `ReEntry_On` / `_Slope_Gate` / `_Close_Gate` | 三者都必須是 1，二次進場這條腿撐過了母體更換 |
| `Tail_ForceExit_Time` | 430 vs 440 值 **+156,000**（含 `Tail_NoEntry_From` 的連動） |

---

## 5. 剩餘批次規格（B4–B13）

**凍結表 ＝ v1.21.0 全預設，明天開檔不用改任何東西。**

```
Slope_Form             = 2        ZLEMA_Fast        = 25
MinSlope_Rate          = 0.050    ZLEMA_Slow        = 70
Slope_Require_Adjacent = 0
Gap_Filter_On          = 1        Gap_Down_Min_Pct  = 0.10
Gap_Day_On             = 1        Gap_Rise_Min_Pct  = 0.20
Gap_Night_On           = 1        Gap_Scope_Bars    = 3
Tail_NoEntry_From      = 415      Tail_ForceExit_Time = 430
Tail_Signal_Lead_Bars  = 2        Sess_LastBar_Block_On = 1
ReEntry_On             = 1        ReEntry_Slope_Gate  = 1
ReEntry_Structure_Gate = 1        ReEntry_Close_Gate  = 1
```

批次順序 ＝ **出場觸發優先序**。先觸發的截斷後面全部，所以由上往下調：

`Kill > Registry > Holiday > Settlement > TailFlat > QuickStop_Loss > QuickStop_Time > ML > Struct/Trail/BE > GoldenCross > TimeStop > SL`

### B4 — QuickStop（330 組）★ 最大失血

| 參數 | 現值 | 起始 | 結束 | 步進 | 格 |
|---|---|---|---|---|---|
| `QuickStop_On` | 1 | 0 | 1 | 1 | 2 |
| `QuickStop_MaxBars` | 4 | 2 | 12 | 1 | 11 |
| `QS_MaxLoss_Pct` | 0.25 | 0.10 | 0.80 | 0.05 | 15 |

**下限 0.10 是硬底**：0.10% × 2020 指數 10,000 = 10 點 = 來回滑價，剛好打平，再低就是掛停損標籤仍淨虧。

要回答的問題：門檻該**放寬讓它有空間**、還是**收緊早點認賠**。
前一批母體裡 QuickStop 兩支合計 −1,908,800、佔 70% 的交易，而 MAE 中位 −30,000（0.33%）**穩定超出 0.25% 門檻**。

### B5 — M8 時間出場（40 組）★ 利潤來源

| 參數 | 現值 | 起始 | 結束 | 步進 | 格 |
|---|---|---|---|---|---|
| `M8_On` | 1 | 0 | 1 | 1 | 2 |
| `MaxHoldingBars` | 24 | 6 | 120 | 6 | 20 |

**若時間只夠跑一組，跑這組。** 24 根 = 2 小時，而 B1 報表前 20 名的
**Avg Bars in Winner 全部落在 18–25，緊貼天花板** —— 時間上限正在整片削掉贏家分布的右尾。
範圍拉到 120 根（10 小時，跨完整夜盤）。

### B6 — BE 保本（180 組）

| 參數 | 現值 | 起始 | 結束 | 步進 | 格 |
|---|---|---|---|---|---|
| `BE_On` | 1 | 0 | 1 | 1 | 2 |
| `BE_Trigger_Pct` | 0.80 | 0.30 | 2.00 | 0.10 | 18 |
| `BE_Cost_Pts` | 10 | **10** | 30 | 5 | 5 |

**`BE_Cost_Pts` 硬下限 10 點**（來回滑價 2,000 ÷ 200）。低於 10 掛保本標籤仍淨虧，v1.11.0 實證。**不可掃 < 10。**

### B7 — Trail 移動停利（418 組）

| 參數 | 現值 | 起始 | 結束 | 步進 | 格 |
|---|---|---|---|---|---|
| `Trail_On` | 1 | 0 | 1 | 1 | 2 |
| `Trail_Trigger_Pct` | 2.00 | 0.50 | 5.00 | 0.25 | 19 |
| `Trail_Giveback_Pct` | 40 | 20 | 70 | 5 | 11 |

> ⚠️ **方向性警告**：依 `feedback_trend_let_profits_run` / `portfolio-role-design`，
> 趨勢類策略**不可為平滑度收緊回檔保護**。20/25/33 保留是為了**測量**，
> **預期採用方向是放寬（50/60/70）**。收緊 giveback 會直接砍掉肥尾。

### B8 — Struct 結構停利（9,072 組）

| 參數 | 現值 | 起始 | 結束 | 步進 | 格 |
|---|---|---|---|---|---|
| `Struct_On` | 1 | 0 | 1 | 1 | 2 |
| `Struct_Lookback` | 20 | 10 | 50 | 5 | 9 |
| `Struct_Buffer_Pts` | 20 | **10** | 50 | 5 | 9 |
| `Struct_UseClose` | 1 | 0 | 1 | 1 | 2 |
| `Struct_Trigger_Pct` | 1.00 | 0.30 | 3.00 | 0.10 | 28 |

`Struct_Lookback ≤ 99`（MaxBarsBack 100）。`Struct_Buffer_Pts ≥ 10`（滑價）。

### B9 — 棘輪三者聯合細掃（27 組）

BE / Struct / Trail **寫入同一個 `v_BE_Stop_Level`**，依觸發百分比先後咬合，**不是獨立參數**。
取 B6 / B7 / B8 各自前 3 名做 3 × 3 × 3 交叉。

### B10 — ML 多層停損（254 組）

| 參數 | 現值 | 起始 | 結束 | 步進 | 格 |
|---|---|---|---|---|---|
| `ML_On` | 1 | 0 | 1 | 1 | 2 |
| `ML_ActivationPct` | 20 | 10 | 40 | 5 | 7 |
| `ML_ScoreTrigger` | 60 | 40 | 80 | 5 | 9 |
| `ML_MinCategories` | 3 | 2 | 5 | 1 | 4 |

上一批母體裡 ML 只作動 **1 筆（−15,200）**。其餘 7 個是偵測器內部量測長度、不是決策點，**第一輪凍結**：
`ML_VolAvgLen` 15、`ML_VolSpikeMult` 2.0、`ML_MA_ShortLen` 10、`ML_ATR_ShortLen` 3、`ML_ATR_LongLen` 90、`ML_ATR_Ratio` 2.5

若 `ML_On` 錨點顯示 ML 有實質作用才開第二輪：
`ML_VolAvgLen` 10–30 步 5｜`ML_VolSpikeMult` 1.5–3.5 步 0.5｜`ML_MA_ShortLen` 5–20 步 5｜
`ML_ATR_ShortLen` 2–6 步 1｜`ML_ATR_LongLen` 60–90 步 10（**上限 99**，現值 90 已是全檔最長回看）｜`ML_ATR_Ratio` 1.5–4.0 步 0.5

### B11 — ATR ／ SL 最後防線（2,880 組）

| 參數 | 現值 | 起始 | 結束 | 步進 | 格 |
|---|---|---|---|---|---|
| `ATR_Len` | 14 | 8 | 30 | 2 | 12 |
| `StopATRMult` | 4.0 | 1.5 | 7.0 | 0.5 | 12 |
| `SL_Pct` | 1.00 | 0.25 | 5.00 | 0.25 | 20 |

`SL_Pct` 範圍即 Rule #12 正式規格（0.00–5.00 步 0.25），取**收斂點** = 最窄的不降效值。

### B12 — Tier2（108 組）

| 參數 | 現值 | 起始 | 結束 | 步進 | 格 |
|---|---|---|---|---|---|
| `Tier2_On` | 0 | 0 | 1 | 1 | 2 |
| `Tier2_Trigger_Pct` | 2.00 | 1.00 | 5.00 | 0.50 | 9 |
| `Tier2_Giveback_Pct` | 40 | 20 | 70 | 10 | 6 |

現為關閉。新母體下重新評估是否啟用。

### B13 — 確認回圈（≈ 85 組）

用 B4–B12 的最終組態，重跑 B1 的 `MinSlope_Rate` 全範圍（0.020–0.100 步 0.005）
＋ `ZLEMA_Fast/Slow` 前 3 名交叉。
名次不變 → 收斂。名次改變 → 以新贏家重跑 **B4 與 B5**（槓桿最高兩批），最多一輪。

---

## 6. 不進最佳化（13 個）

| 參數 | 值 | 理由 |
|---|---|---|
| `Slope_Form` | 2 | 本次 campaign 的定義 |
| `MinSlope` | 28 | Form 2 下走 else 分支，**已惰性** |
| `Settlement_Flat_Time` | 1230 | Rule #11 硬規則 |
| `Holiday_Flat_Time` | 415 | 假日前平倉鐵律 |
| `Registry_Valid_Until` | 1270101 | 註冊表 |
| `Manual_Kill_Switch` | False | 營運開關（且維持 `TrueFalse` 型） |
| `Sess_Day_Start/End` | 845 / 1345 | 期交所時段定義 |
| `Sess_Night_Start/End` | 1500 / 500 | 期交所時段定義 |
| `KB_Body_Long/Mid/Small`、`KB_Shadow_Ratio` | 0.75/0.30/0.05/2.00 | `v_KB_Type` **寫 18 次讀 0 次，未接線，掃了不會有任何變化** |

`KB_*` 與 `Tier2_*` 要啟用是**新功能開發**，不是參數最佳化，分開處理。

---

## 7. 執行紀律（每一格都適用）

| # | 規則 |
|---|---|
| 1 | **一格一變因**。禁止順手一起改，會讓歸因不可能 |
| 2 | **比較用 `(日期, 時間, 進場價)` 配對**，不用交易編號。筆數變動時編號整體位移，按編號比會產生虛假的「每筆都變了」 |
| 3 | **取高原中心，不取尖峰**。相鄰兩格淨利差 > 40% 不採用；採用值須有**兩側各一格**同向支撐 |
| 4 | **邊界上的最佳值不是最佳值**，是截斷。往外延伸掃到出現真轉折為止（B3 → B3b 就是這條） |
| 5 | **時間類參數只取合法時鐘時間**，掃描步進須讓每格落在 00–59 分內（見 §3.2） |
| 6 | **最佳值回寫 `.pla` input 預設**（`feedback_param_must_sync`），code = deploy = docs |
| 7 | **主目標淨利**；副目標調整後 PF > 1.3、最大策略虧損 % < 25% |

**鏈式相依提醒**：`v_ReEntry_Price = v_Last_EntryPrice`，父單被擋整條鏈消失。
二次進場相關的任何比較，筆數變動會超過直覺，必須用紀律 2 的配對法。

---

## 8. 保留意見

1. **績效起點是 748,000，不是 3,098,400。** 採用 Form 2 是**公式正確性**的裁示。
   即使全部調完也不保證回到 300 萬。這一點必須隨版本攜帶，**不可事後被重新詮釋成「最佳化的成果」**。

2. **`QuickStop_Loss` 的訂單型態缺陷未解。** 價格條件（`QS_MaxLoss_Pct`）卻用市價單，
   MAE 中位 −30,000（0.33%）穩定超出 0.25% 門檻，37/38 筆超穿。
   **參數掃描修不了這件事**，改 stop 單是獨立的程式碼改動。
   B4 掃出來的最佳 `QS_MaxLoss_Pct` **會混入這個滑移，須標註**。

3. **`ZLEMA_Fast/Slow` 改動會改變死叉集本身**，與 `MinSlope_Rate` 耦合。
   本計畫用座標下降 ＋ B13 回圈處理，這是**近似解不是聯合最佳解**。
   聯合最佳解在 6.4 年樣本上必定過擬合，**刻意不做**。

4. **零 OOS 資料剩餘。** 這策略已在 2020–2026 全樣本上做過約 25 個選擇，本 campaign 再加。
   Form 2 讓年度分布第一次成立（2021 從 0 筆變 13 筆、2023 從 0 筆變 2 筆），
   **切 holdout 在技術上第一次可行** —— 尚未執行，B13 之後再議。

5. **Rule #13 十維度 ／ Rule #18 五件套 ／ W4 WFA 全部未跑。** 這些是 PROMOTE 的前置，不是本 campaign 的範圍。

---

## 9. 檔案與 Git

| 項目 | 位置 |
|---|---|
| 策略 | `strategies/research/S16_MACrossShort/S16_S_MACrossShort_v1.21.0.pla` |
| 本文件 | `docs/research/S16S_v1200_form2_reoptimization_plan_20260812.md` |
| 前一版（含 v1.18.0 錨點） | git commit `185cf66` 的 `S16_S_MACrossShort_v1.20.0.pla` |
| 離線 ZLEMA 重建腳本 | scratchpad `rate_curve.py`（1 分 K 來源 `MC9_20260108/報價/TXF1 1 分鐘.txt`） |

**v1.18.0 的 118 筆／3,098,400 已不可靠單一 input 切回**（gap 預設也動了），
要回去請取 commit `185cf66`。舊錨點的任務已經完成。
