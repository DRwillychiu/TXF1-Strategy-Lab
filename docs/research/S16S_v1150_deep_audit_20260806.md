# S16_S v1.15.0 深度檢驗：函數 / 參數 / 標籤（2026-08-06 第二輪）

**對象**：`S16_S_MACrossShort_v1.15.0.pla`
**與第一輪的差別**：第一輪查**狀態機與合規**，本輪查**函數用法、參數依據、標籤設定**。
**方法**：程式化掃描 + 逐項人工複查（掃描器有誤判，全部人工驗證後才列入）。

---

## 0. 摘要

| 項目 | 結果 |
|---|---|
| 掃描器誤判（已排除） | **3 項** |
| 真發現 | **6 項** |
| 已於本輪修正 | **1 項**（B-1 過時註解） |
| 標籤設定 | **14 個全部合規**，零重複、零命名違規、動詞前綴 100% 一致 |
| Input 死參數 | **0 個**（41 個全部有被引用） |
| 變數死賦值 | **0 個**；死宣告 **1 個**（B-3） |

**最有價值的發現是 B-6** —— `BE_Trigger_Pct = 0.80` 與今天修掉的兩個參數是**同型問題**，
可能同樣已是死參數，值得用同一招驗證。

---

## 1. 掃描器誤判（誠實記錄，避免下次重踩）

| 誤報 | 實情 | 誤判原因 |
|---|---|---|
| ML 前置計算在條件式內 | L1196-1200 在**頂層**，每根都算 | 深度計數器對前一區塊的 `end;` 誤計 |
| `hidx` 從未寫入 | 它是 `for hidx = 1 to 80` 的迴圈變數 | 正則只認 `^\s*hidx\s*=` |
| `SX_MA_BE` / `SX_MA_Trail` 從未掛單 | 在 if/else 鏈中確實有掛 | 正則要求語句結尾分號，if/else 前兩支沒有 |

**教訓**：靜態掃描的每一個 flag 都必須人工複查原始碼才能寫進報告。

---

## 2. 函數使用檢驗

### 2.1 Series function 的每根求值紀律

PowerLanguage 中具內部狀態的函數（RSI、XAverage 等）需**每根 K 棒求值**，
否則內部序列會陳舊。逐一檢查：

| 函數 | 位置 | 巢狀深度 | 判定 |
|---|---|---|---|
| `XAverage` ×2（ZLEMA） | L1071-1072 | 0 | **每根，正確** |
| `AvgTrueRange(ATR_Len)` | L1122 | 0 | **每根，正確** |
| `Average` ×2 / `AvgTrueRange` ×2 / `RSI`（ML 前置） | L1196-1200 | 0 | **每根，正確** |
| `Lowest( RSI(Close,14), 5 )` | L1335 | 條件式內 | **B-2，見下** |
| `Highest( Close \| High, v_Struct_Window )` | L1483/1485 | 條件式內 | **B-5，見下** |

### 2.2 B-2　`Lowest( RSI(Close,14), 5 )` 寫在條件式內

**位置**：L1335，位於 `if v_Loss > v_ML_Activation_Level then` 區塊內。

**風險比第一輪 A-3 所述為低，此處修正：**

- `v_ML_RSI = RSI( Close, 14 )` 已在 L1199 **每根求值**
- MC 對相同引數的函數呼叫**每根只求值一次並快取**，故內層 `RSI(Close,14)` 讀到的是
  已完整維護的序列
- `Lowest()` 本身無內部狀態，只是掃描既有序列

**所以「陳舊值」這個失效模式已被 L1199 擋掉。** 第一輪把它講得比實際嚴重，在此修正。

**仍建議清理**：寫法依賴「另一行剛好每根都算」這個隱性條件，
若日後有人移除 L1199（例如拆掉 ML），這條就會真的變成裸露的條件式呼叫。
移到 Section 8 前置計算即可根除。

**目前影響：零**（ML 在 129 筆中觸發 0 次）。

### 2.3 B-5　變長度 `Highest( Close, v_Struct_Window )`

```powerlanguage
v_Struct_Window = MinList( Struct_Lookback, v_BarsSince + 1 );
if v_Struct_Window < 1 then v_Struct_Window = 1;
```

長度受**雙重限制**：`Struct_Lookback`（現值 20）與 `v_BarsSince + 1`（受 MaxHoldingBars=24 封頂）。
實際範圍 **1..20**，遠低於 MaxBarsBack = 100。

`Highest()` 直接讀 Close/High 資料序列，**無內部狀態，條件式呼叫安全**。

**風險僅在未來**：若把 `Struct_Lookback` 掃到 99 以上會超出 MaxBarsBack。
現行掃描上限 40，安全。**若日後放寬掃描範圍，需同步提高 MaxBarsBack。**

### 2.4 回看深度 vs MaxBarsBack = 100

| 來源 | 深度 |
|---|---|
| `AvgTrueRange( ML_ATR_LongLen )` | **90**（最深） |
| `XAverage( ..., ZLEMA_Slow )` | 70 |
| `Close[ v_Slow_Lag ]`（ZLEMA 位移） | 34 |
| `Highest( Close, v_Struct_Window )` | ≤ 20 |

**餘裕 10 根，由 ML 撐住上限。** 若移除 ML 模組，最深降為 70，餘裕變 30。

> `XAverage` 為遞迴實作（只需前一根自身值），**不會**在 `Close[34]` 之上再疊 70 根。
> 策略在 MaxBarsBack=100 下實際運行無誤，此為實證而非推論。

---

## 3. 參數規劃：41 個 input 的依據全表

### 3.1 依據強度分級

| 等級 | 定義 |
|---|---|
| **A** | 在**現行架構**下經掃描驗證 |
| **B** | 經掃描，但**在舊架構下**（有 provenance 落差） |
| **C** | 算術推導 / 平台常數，非最佳化 |
| **D** | **從未掃描**，沿用預設或設計判斷 |
| — | 純開關，無數值 |

### 3.2 全表

| # | Input | 現值 | 級 | 依據 | 最後驗證 |
|---|---|---|---|---|---|
| 1 | `ZLEMA_Fast` | 25 | B | v0.5 GA 1024 組 | v0.5 |
| 2 | `ZLEMA_Slow` | 70 | B | v0.5 GA，**窄峰**（S80 = −33.5%） | v0.5 |
| 3 | `MinSlope` | 28 | B | v0.5 GA，26-28 高原 / 30 懸崖 | v0.5 |
| 4 | `QuickStop_On` | True | — | 開關 | — |
| 5 | `QuickStop_MaxBars` | 4 | B | v0.5 | v1.10.0 |
| 6 | `QS_MaxLoss_Pct` | 0.25 | B | v1.7.1 掃描 | v1.7.1 |
| 7 | `ML_On` | True | — | 開關 | — |
| 8-16 | `ML_*`（9 個） | — | **D** | Rule #17 SOP 預設值 | **從未掃描，129 筆 0 觸發** |
| 17 | `BE_On` | True | — | 開關 | — |
| 18 | **`BE_Trigger_Pct`** | **0.80** | **B** | v1.11.0 80 組掃描，高原 0.70-1.00 | **v1.11.0（無 Trail / 無 Struct / 無 Tier2 時）← B-6** |
| 19 | `BE_Cost_Pts` | 10 | **C** | 算術：滑價 2,000/口 ÷ 200 = 10 點 | v1.12.0 |
| 20 | `Trail_On` | True | — | 開關 | — |
| 21 | `Trail_Trigger_Pct` | 2.00 | **A** | v1.15.0 塌縮，位元等價證明 | v1.15.0 |
| 22 | `Trail_Giveback_Pct` | 40 | **A** | v1.14.0 B1 25 組掃描 | v1.14.0 |
| 23 | `Struct_On` | True | — | 開關 | — |
| 24 | `Struct_Lookback` | 20 | **A** | v1.13.0 掃描（**25+ 因 clamp 等價**） | v1.13.0 |
| 25 | `Struct_Buffer_Pts` | 20 | **D** | **操作判斷，非最佳化**（0 分數最高但必被掃損） | v1.13.0 |
| 26 | `Struct_UseClose` | True | **A** | **P2 對照，收盤價決定性勝出** | 2026-08-05 |
| 27 | `Struct_Trigger_Pct` | 1.00 | **A** | v1.15.0 11 格窮舉，高原中心 | v1.15.0 |
| 28-30 | `Tier2_*`（3 個） | 退役 | **A** | v1.15.0 塌縮，位元等價 | v1.15.0 |
| 31 | `M8_On` | True | — | 開關 | — |
| 32 | `MaxHoldingBars` | 24 | **A** | v1.14.0 14 值掃描 + 無限制對照 | v1.14.0 |
| 33 | `ATR_Len` | 14 | **D** | 業界標準預設 | **從未掃描** |
| 34 | `StopATRMult` | 4.0 | **D** | v0.5 沿用 | **從未單獨掃描** |
| 35 | `SL_Pct` | 1.00 | B | v1.5 掃描 0-5.0 step 0.25，收斂點 | v1.5 |
| 36 | `Holiday_Flat_Time` | 415 | **C** | 5M 網格對齊（2026-08-05 稽核確認） | 2026-08-05 |
| 37 | `Registry_Valid_Until` | 1270101 | **C** | TAIFEX 行事曆邊界 | — |
| 38 | `Manual_Kill_Switch` | False | — | 開關 | — |
| 39 | `Settlement_Flat_Time` | 1230 | **C** | Rule #11 憲法規定 | — |
| 40 | `Tail_LastEntry_Time` | 430 | **D** | v1.3 設計判斷 | **從未掃描** |
| 41 | `Tail_ForceExit_Time` | 440 | **D** | v1.3 設計判斷 | **從未掃描** |

### 3.3 分級統計

```
A 現行架構已驗證   7 個   （今天新增 3 個）
B 舊架構下驗證     6 個   ← provenance 落差
C 算術 / 常數      4 個
D 從未掃描        14 個   （其中 ML 佔 9 個）
—  純開關          10 個
```

**D 級 14 個中，ML 佔 9 個。若移除 ML 模組，D 級立刻降到 5 個。**

---

## 4. 標籤設定檢驗

### 4.1 全部 14 個標籤

| 標籤 | 動詞 | 訂單型態 | ExitFired |
|---|---|---|---|
| `SE_MA_DeathCross` | sell short | market | —（進場） |
| `SE_MA_ReEntry` | sell short | **stop** | —（進場） |
| `SX_MA_Kill` | buy to cover | market | 設 |
| `SX_MA_Registry` | buy to cover | market | 設 |
| `SX_MA_Holiday` | buy to cover | market | 設 |
| `SX_MA_Settlement` | buy to cover | market | 設 |
| `SX_MA_TailFlat` | buy to cover | market | 設 |
| `SX_MA_QuickStop_Loss` | buy to cover | market | 設 |
| `SX_MA_QuickStop_Time` | buy to cover | market | 設 |
| `SX_MA_ML_Exit` | buy to cover | market | 設 |
| `SX_MA_Struct` | buy to cover | **stop** | **不設**（設計如此） |
| `SX_MA_Trail` | buy to cover | **stop** | **不設**（設計如此） |
| `SX_MA_BE` | buy to cover | **stop** | **不設**（設計如此） |
| `SX_MA_GoldenCross` | buy to cover | market | 設 |
| `SX_MA_TimeStop` | buy to cover | market | 設 |
| `SX_MA_SL` | buy to cover | **stop** | **不設**（設計如此） |

### 4.2 合規檢查

| 檢查 | 結果 |
|---|---|
| 重複標籤 | **0** |
| `SE_` / `SX_` 前綴 | **14/14 合規** |
| 動詞與前綴一致性（memory `feedback_mc_entry_exit_labels`） | **100%**：`SE_` 全為 `sell short`、`SX_` 全為 `buy to cover` |
| 標頭文件 vs 實際發出 | **完全一致**，零漏列零多列 |
| `ExitFired` 紀律 | market 單全部設、stop 單全部不設 —— **與設計一致**（stop 單需每根重掛） |

### 4.3 B-4　P7 引擎停損沒有標籤（新發現）

`SetStopLoss` 是 MC **引擎層級**停損，觸發時**不會帶 `SX_` 標籤**。

現行 129 筆的出場標籤總和：
```
25 + 4 + 3 + 3 + 2 + 2 + 2 + 44 + 44 = 129   全部有標籤
```

**代表引擎停損在 6.4 年中從未觸發** —— 它確實是最後防線，前面六層都先接住了。

**但這是出場歸因的盲點**：若哪天引擎停損真的觸發，該筆交易在報表中不會有可辨識的
出場標籤，逐筆歸因會斷掉。屬已知限制，非缺陷（引擎層級停損本就無法自訂標籤）。

**建議**：在 promote 後的實盤監控中，把「出場標籤為空」列為告警條件。

---

## 5. 本輪的真發現彙整

| # | 發現 | 嚴重度 | 處置 |
|---|---|---|---|
| **B-1** | **`Struct_UseClose` 註解過時**：三個版本都寫「STILL UNSWEPT (P2 pending)」，但 P2 已於 2026-08-05 完成且結果決定性 | **中** | **本輪已修**，並寫入 P2 的實測結論 |
| **B-2** | `Lowest(RSI(...),5)` 在條件式內 | 低 | 未動。風險已被 L1199 的每根求值擋掉；**第一輪 A-3 的嚴重度在此下修** |
| **B-3** | `v_ML_Loss` 為死變數（L940 宣告，全檔零引用） | 低 | 未動。隨 ML 模組一併移除 |
| **B-4** | P7 引擎停損無標籤 → 出場歸因盲點 | 低 | 記錄。建議列入實盤監控告警 |
| **B-5** | 變長度 `Highest()` 在 `Struct_Lookback > 99` 時會超出 MaxBarsBack | 低 | 記錄。現行掃描上限 40，安全 |
| **B-6** | **`BE_Trigger_Pct = 0.80` 與今天修掉的兩個參數同型** | **高（機會）** | **見下** |

---

## 6. B-6：下一個該驗的參數

### 6.1 問題

`BE_Trigger_Pct = 0.80` 出自 v1.11.0 的 80 組掃描，高原 0.70-1.00。**掃描本身沒問題。**

問題在於**當時的架構**：

| 架構元素 | v1.11.0 掃 0.80 時 | 現行 v1.15.0 |
|---|---|---|
| 保本腿 | **唯一的保護** | 三條腿之一 |
| 百分比停利腿 | 不存在 | 存在（2.00/40） |
| 結構停利腿 | 不存在 | 存在（1.00/20/20） |

**這與 `Struct_Trigger_Pct`（從隔壁腿抄來）、`Trail_Giveback_Pct`（掃於無分層時代）
是完全同一類問題** —— 在舊架構下最佳化的值，沿用到新架構卻沒重驗。

### 6.2 懷疑：它可能已是死參數

程式碼取三條腿鎖利的**最大值**：

```
保本      鎖 BE_Cost_Pts = 10 點（固定）
百分比腿  鎖 峰值 × 60%（峰值 ≥ 2.00% 時）
結構腿    鎖 窗口內最小獲利 − 20 點（峰值 ≥ 1.00% 時）
```

**保本鎖的是固定 10 點。** 只要另外兩條腿任一武裝且鎖利 > 10 點，保本就輸。

結構腿在峰值 1.00% 就武裝（比保本的 0.80% 晚一點），而它的鎖利通常遠大於 10 點。

**所以保本可能只在「峰值介於 0.80% 與 1.00% 之間」這個極窄帶才是贏家。**

現行資料：`SX_MA_BE` 出場 **2 筆，損益合計 0**。兩筆都是剛好打平出場。

### 6.3 建議的驗證（與今天完全同一套方法）

**失效測試**：把 `BE_Trigger_Pct` 設到 **99**（門檻高到永不觸發），其餘全部不動。

| 結果 | 意義 |
|---|---|
| **逐筆位元相同** | 保本腿是死的 —— 但**不建議移除**，它是「不倒賠」的語意保證，留著當地板 |
| 那 2 筆變成別的出場 | 保本確實在管那個窄帶，記錄依據，收案 |

**成本：1 次回測。**

> 注意：即使證實為死參數，**保本腿不該移除**。它與另外兩個不同 ——
> `Trail_Giveback_Pct` 是純數值調校，保本是**風險語意**（保證不倒賠）。
> 驗證的目的是「知道它現在管到什麼」，不是「砍掉它」。

---

## 7. 誠實聲明

- 本輪為**靜態分析 + 既有回測資料交叉驗證**，未執行 MC12 編譯。
- §2.2 對 MC 函數快取行為的判斷依據為 PowerLanguage 語言規範，**未以實機驗證**。
- §2.4 的 `XAverage` 遞迴實作判斷同上，但「策略在 MaxBarsBack=100 下實際運行無誤」
  是實證。
- §3.2 的依據等級依現有 repo 文件記載彙整；標示「從未掃描」者為
  **在 docs/ 中找不到掃描證據**，不排除掃描曾經做過但未留檔。
- B-6 的「保本可能是死參數」為**推論**，未經實測。§6.3 的失效測試尚未執行。
