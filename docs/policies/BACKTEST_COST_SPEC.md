# 回測成本與策略屬性規格（2026-08-04 建立）

本檔為 CLAUDE.md「技術規格」段落的完整版。所有策略（含 `strategies/live/` 已上架者）共用同一組 MC 策略屬性設定。

---

## 1. 物理證據來源

2026-08-04 使用者提供 MultiCharts 12「策略屬性」對話框截圖，內容如下（逐項照抄）：

| 欄位 | 設定值 |
|------|--------|
| 手續費規則 | 無手續費 |
| 滑價 | 1000，選項為「**每股/每口**」（非「每筆交易」） |
| 原始資金 | 2,000,000 |
| 利率 | 2 % |
| 策略運算最大使用K棒數量 | 100 |
| 基礎貨幣 | None |
| 最多容許同向委託 | **未勾選**（平台不限制加碼） |
| 個別商品最大部位數量 | 20 |
| 委託數量 | 固定委託股數(口數) = **2** |
| 無條件捨去至 | 1 口 |
| 最低委託股數(口數) | 0 |

---

## 2. 成本換算（本檔的核心）

MultiCharts 的滑價在「每股/每口」模式下 **每一邊各收一次**（進場收一次、出場收一次）。

```
單邊單口成本 = 1,000 NTD = 5 點（1,000 / 200）
來回單口成本 = 2,000 NTD = 10 點
來回 2 口成本 = 4,000 NTD
```

### 鐵則

> **任何代表「成本」「保本」「最小獲利」的點數常數，必須 >= 10 點。**
> 低於 10 點的保本出場，即使掛著保本標籤，實際仍是淨虧損。

### 實證（S16_S v1.11.0）

`BE_Cost_Pts = 5` 時：

```
毛利 = 5 點 × 200 NTD × 2 口 = 2,000 NTD
成本 = 1,000 NTD × 2 邊 × 2 口 = 4,000 NTD
淨額 = -2,000 NTD
```

回測 129 筆中的 5 筆 BE 出場，淨損益 **全部精確等於 -2,000 NTD，無一例外**。
另反推 10 筆交易的隱含成本，亦全部精確等於 4,000 NTD。

v1.12.0 修正為 `BE_Cost_Pts = 10` 後，該 5 筆變為 net 0，Net 由 2,478,000 → 2,488,000。

---

## 3. 與現實的落差（回測屬保守壓測）

真實 TXF1 交易成本約略為：

| 項目 | 現實值（每口每邊） |
|------|-------------------|
| 手續費 | 約 30 ~ 50 NTD |
| 滑價（流動時段） | 約 1 點 = 200 NTD |
| 滑價（夜盤淺盤） | 約 1 ~ 3 點 |

回測設定的 1,000 NTD 每口每邊 ≈ 5 點，約為現實的 **3 ~ 5 倍**。

**結論：回測結果是壓測值，非樂觀值。** 實盤成本應低於回測假設，此為安全方向。
但 `BE_Cost_Pts = 10` 是校準到回測假設，實盤時該價位會比真實保本點更遠，
效果是持倉略久一點，非虧損風險。

---

## 4. MaxBarsBack 約束

### 4.1 它是什麼（2026-08-21 用戶確認，U7 結案）

MC12 的圖表屬性「策略運算最大使用 K 棒數量」。**定義是暖機需求**：
系統先跑 X 根才開始產生回測交易筆數。**不是程式碼，`.pla` 檔內沒有這一行，刪不掉也不該刪。**

**用戶實際設定：100。** 這個值由用戶在 MC12 介面設定，全策略一致。

### 4.2 U7 爭議結案

| 出處 | 值 | 判定 |
|---|---|---|
| 專案 CLAUDE.md | 100 | ✅ **正確** |
| `S16_S_MACrossShort_v1.17.0.pla:984` | `>= 200` | ❌ **錯誤註解**，歷史版本檔不修改，在此標記 |
| `REENTRY_MODULE_EXTRACTION_20260808.md` U7 | 未知 | ✅ **本節結案** |

> 附註：U7 原文引 `:923`，實際在 `:984`。連行號都引錯，是 U7 拖 13 天未結的原因之一。

### 4.3 ★ 本節舊內容已作廢 —— 記錄原因，避免重犯

**舊內容寫「餘裕僅約 9 根」，依據是 `AvgTrueRange( ML_ATR_LongLen )` 的 90 根回看。**
**ML Monitor 模組已移除。該敘述自模組移除起就是錯的，但文件未同步。**

### 4.4 現況（v1.26.0 實測）

| 指標 | 長度 | 實際回看 |
|------|------|---------|
| `Struct_Lookback` | 20 | ~20 根 |
| `ATR_Len` | 14 | ~15 根 |

**最深回看約 20 根，設定 100 → 餘裕約 80 根。**

### 4.5 調高的代價（供未來圖表型態研究參考）

資料共 **418,096** 根 5 分 K。提高 MaxBarsBack 只多燒暖機根數：

| 100 → | 多燒 | 佔全資料 |
|---|---|---|
| 200 | 100 根 | 0.02% |
| 400 | 300 根 | **0.07%** |
| 1000 | 900 根 | 0.22% |

**代價可忽略。圖表型態需要多少根就設多少根，不需把它當約束。**

> ⚠️ **教訓（2026-08-21）**：本節舊版被當成「硬約束」引用，用來論證
> 「99 根塞不下頭肩頂，所以複合頭肩頂不可實作」。**那個結論作廢。**
> **一個數字被當成約束之前，必須先回答：它是定義、量測、還是抄來的慣例？**

---

## 5. 部位限制與加碼

平台端「最多容許同向委託」**未勾選**，個別商品上限 20 口。
即 MC 本身不阻止加碼（2 口 × 10 次 = 20 口）。

**防線在策略程式碼**：各策略進場條件皆以 `MarketPosition = 0` 為前提。
S16_S v1.12.0 已驗證：主進場與 re-entry 皆有此守衛
（`S16_S_MACrossShort_v1.12.0.pla:709`）。

> 新策略開發時，若進場條件未含 `MarketPosition = 0`，平台不會擋，會直接疊倉。

---

## 6. 舊版錯誤記錄（供追溯）

2026-08-04 之前，CLAUDE.md 技術規格記載為：

```
- 滑價：1,000 NTD round-trip（單邊 500）   <- 錯誤，實際單邊 1,000
- 固定口數：1 口                            <- 錯誤，實際 2 口
```

滑價認知差 **2 倍**，口數差 **2 倍**。
在此日期之前，所有依「單邊 500」推導的成本門檻常數皆偏低，需逐一複查。
複查結果見 `docs/research/` 對應稽核報告。

備份：`CLAUDE.md.bak_20260804`
