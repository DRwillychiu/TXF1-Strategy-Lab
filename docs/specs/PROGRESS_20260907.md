# TXF1 Python 移植　進度回報

**2026-09-07（起 2026-09-02，第 6 天）**

---

## 一 完成度

| | |
|---|---|
| **策略移植** | **5 / 5 完成**（L1 · L2 · L3 · L4 · L5） |
| **套件** | 17 / 20 有內容 |
| **測試** | **270 通過** |
| **移植稽核** | **5 支零落差** |

---

## 二 移植稽核（`tools/audit_port.py`，機器比對 `.pla` vs Python）

```
✓ L1   inputs 23   訂單標籤 12   from Entry  0   落差 0
✓ L2   inputs 22   訂單標籤 14   from Entry  0   落差 0
✓ L3   inputs 20   訂單標籤 10   from Entry  0   落差 0
✓ L4   inputs 23   訂單標籤 11   from Entry  0   落差 0
✓ L5   inputs 24   訂單標籤 24   from Entry 28   落差 0
                   ───────────
                   112 個 input · 71 個訂單標籤 · 全數逐項比對
```

**每個 input 的名稱與預設值、每個下單標籤、每處 `from Entry` 綁定，
都與原始 `.pla` 逐項比對通過。**

---

## 三 回測對帳（對 MC12 anchor）

| 策略 | 版本 | 回測視窗 | Python | MC12 | 差 | PF | MC PF |
|---|---|---|---|---|---|---|---|
| **L4** | v14.7 | 2019-12-16 ~ 2026-08-26 | 83 | 82 | **+1** | 1.922 | 1.415 |
| **L2** | v5.4 | 2019-12-16 ~ 2026-08-25 | 80 | 77 | **+3** | 3.111 | 2.746 |
| **L3** | v15.0 | 2020-05-12 ~ 2026-07-25 | 379 | 376 | **+3** | 1.319 | 1.255 |
| **L1** | V2.7 | 2019-12-16 ~ 2026-07-02 | 496 | 476 | +20 | 1.560 | 1.496 |
| L5 | v19.6 | ~4.8 年 | 263 | 162 | +101 | 1.004 | 2.136 |

**三支在 +3 筆以內。**

### L4 的出場標籤逐一吻合

| 標籤 | Python | MC | 差 |
|---|---|---|---|
| `CS_Entry` | 79 | 79 | **0** |
| `CS_SL`（含引擎停損） | 38 | 38 | **0** |
| `CS_BreakExit` | 30 | 30 | **0** |
| `CS_TimeExit` | 14 | 14 | **0** |

`CS_BreakExit` 的勝率 **0.0%**，與標頭記載完全一致——
那是 L4「假突破失敗」的核心結構，零違反。

---

## 四 資料層

| | |
|---|---|
| 1 分 K | **2,098,922 列**（2019-01-02 ~ 2026-09-05） |
| 聚合週期 | 15M 141,302 · 45M 48,377 · 60M 35,352 · 日 1,864 · 週 372 |
| 資料完整性 | OHLC 不一致 **0** · 時間倒退 **0** |
| 資料版本守門 | SHA-256 每次執行前驗證 |

### 由資料反推的結算日曆

```
資料反推的結算日   92 天
靜態規則標的       90 天
規則漏標            2 天   2023-01-30 · 2026-02-23（皆為農曆年後遞延）
規則誤標            0 天
```

**五份 `.pla` 都用「第三個週三」的靜態規則，實測會漏標 2 天。**
Python 版用資料反推，超出驗證範圍時退回靜態規則。

---

## 五 不變量檢查（判官 2，不需要對照組）

```
L2   35,313 次檢查   158 次違反，全部是規格表已記錄的落差，零意外
L4  141,103 次檢查   零違反
```

L4 的十條承諾**全數成立**，與靜態閱讀的結論一致。

---

## 六 產出物

| 檔案 | 內容 |
|---|---|
| `reports/backtest_report.html` | 五支權益曲線 + 對 anchor 比較 + 標籤分布 |
| `reports/L1~L5_trades.csv` | 逐筆交易清單，欄位對齊 MC12 交易明細 |
| `reports/summary.csv` | 五支彙總，可直接貼進 Excel |

### 可執行的驗證指令

```powershell
python -m pytest tests\ -q                                    # 270 通過
python tools\audit_port.py                                    # 移植稽核
python tools\report.py "data\mc_export\TXF1 1 分鐘.txt"        # 回測報告
python tools\signals.py "data\mc_export\TXF1 1 分鐘.txt"       # 當前訊號
python tools\verify_claims.py "data\mc_export\TXF1 1 分鐘.txt" # 逐項驗證
python tools\audit.py "data\mc_export\TXF1 1 分鐘.txt"         # 資料與不變量稽核
```

**每一項數字都可由指令重現。**

---

## 七 已知落差（未修，公開列出）

| # | 問題 | 嚴重度 |
|---|---|---|
| **P-1** | **L1 是收盤評估，不是 IOG 逐 tick**——`engine/context.py` 未實作 | ★★★ |
| **P-2** | L5 差 +101 筆，箱體幾何未對上（日線定義待 MC 匯出比對） | ★★★ |
| P-3 | `v_Prev_MP` 在 IOG 下記錄 tick 而非 bar（`ReEntry_On=0` 時惰性） | ★★ |
| P-4 | `SetStopContract` 位置三支不同，需裁決是否為 bug | ★★ |
| P-5 | 換月接續只做標記，未做 back-adjust | ★★ |
| P-6 | L2 差 +3 筆，分解為「3 筆多出來 + 1 筆標籤錯」 | ★ |

---

## 八 尚未開始

```
engine/context   IOG tick 生命週期      擋住 L1 的正確性
runtime/live     dry-run                從 MC12 切換的唯一安全路徑
notify           訊號通知               專案終點的一半
state            崩潰復原
obs              觸發次數監控
journal          事件日誌
broker           自動下單               必須在 dry-run 連續一致之後
```

---

## 九 今日主要進展（2026-09-07）

1. **兩台筆電與雲端 git 完全同步**（解決命名重構造成的分叉，`317a042`）
2. **L3 移植** → 379 vs 376（+0.8%）
3. **發現回測視窗是第一級參數**——L3 從 +23 筆降到 +3 筆
4. **在 repo 內找到真正的 anchor 文件**——修正 L4 的基準（82 不是 79）
5. **L5 · L1 移植** → 五支全部完成
6. **建立四支可執行的驗證工具**（稽核 · 報告 · 訊號 · 資料守門）
7. **稽核抓到並修正四個真缺陷**（見下）
8. **稽核工具本身的缺陷也被抓到並修正**

### 稽核抓到的四個缺陷

| 缺陷 | 後果 |
|---|---|
| L1 六個 re-entry input + `TL_ReEntry` 標籤缺失 | 開關打開時 Python 靜默不動作，MC 會發單 |
| L5 的 24 個標籤用字串組合 | 打錯前綴沒有任何檢查抓得到 |
| L5 的 40% 分批出場從未生效 | Stage 2/3 的追蹤機制完全沒執行 |
| 綁到不存在的腿時中止整批 | L5 部位掛了 3416 根，而時間停損是 31 根 |

### 稽核工具本身的缺陷

PowerLanguage 有**四個**下單動詞，工具只認兩個（漏 `SellShort` / `BuyToCover`），
**對 L2 與 L4 印了「通過」而實際上連看都沒看**。

已修，並加三道防線：四個動詞、**標籤數 0 一律報錯**、標頭 grep 數比對。
