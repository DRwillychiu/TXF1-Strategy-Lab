# HANDOFF 2026-08-27 EOD — S16_S 型態層

**下一台機器：筆電。** 先 `git pull`，然後看第 5 節。

---

## 1. Status

擴散家族**七個變體全部結案**，指標實作到 Build 260843。
**唯一未結的事：MC12 回報 199 個型態，基準是 203。**

| | |
|---|---|
| 型態層進度 | 純圖形 72 種 → 已檢定 33 ＋ 擴散家族 7 ＝ **40 種**，剩 32 種（建議砍 4 種 → 28） |
| 指標 | `IND_S16S_P29.pla` **Build 260843**，尚未在 MC12 驗收 |
| 訊號層 / 策略層 | **未開始**（用戶裁示：型態層走完再一起進） |
| Git | `89962fe`（＋本次收尾 commit），本地／遠端一致 |

---

## 2. Changed（今天做了什麼）

### 研究

| 產出 | 結論 |
|---|---|
| `s16s_p51_p54_scan.py` | P51 1,669／P52 1,717／P53 7／P54 8（對照 P29 203） |
| `s16s_p51_p54_pick.py` | 示意圖真實案例。**首版用「振幅最大」選到 1,819 點跨夜盤極端案例，已改中位數** |
| `s16s_frame_decompose.py` | 32,094 框架 3×3 交叉表。**高低同向 : 反向 ＝ 10.3 : 1** |
| `s16s_break_outcome.py` | 破位四種定義的結果分布 |
| `s16s_p29_break_split.py` | P29 子分類 × 破位，排列檢定 20,000 次 |
| `s16s_frame_decompose` 衍生 | P29 觀測 203 vs 獨立期望 1,247 ＝ **0.16x** |

### 程式碼

| Build | 內容 |
|---|---|
| 260840 | 破位追蹤三槽、白色跌破 K 棒、計數標籤 |
| **260841** | **★ 錨點鎖存**（今天最重要的修正，見第 4 節） |
| 260842 | `v_PvDt`/`v_PvTm` 補進六槽平移；新增 `P29 DIAG` |
| 260843 | 逐年型態直方圖，用來定位少掉的 4 個 |

### 工具

- **`scripts/verify_pla_build_id.py`（新）** —— 檔頭 Build_ID 與 input 不一致就 FAIL。
  首跑抓到兩個，其中 `S16_S_MACrossShort_v1.26.0.pla` 從 8/25 就錯（檔頭 260826、input 260825）。
  **那個檔自己的檔頭寫著「六個批次因為 Build_ID 對不上被作廢」—— 這個不一致會害人誤判 MC 載錯檔。**

---

## 3. State（現在的狀態）

### 已結案：擴散家族七變體

完整記錄在 `docs/research/S16S_BROADENING_FAMILY_CLOSED_20260827.md`。

**自由參數合計 0 個。**

> ### 永久結論（適用於之後每一個型態）
> **破位方向 ＝ 起算位置 ＋ 低點方向延續。兩者都不是型態資訊。**
> 研究新型態時**不必再測「它往哪邊破」**。若型態有價值，價值必然在
> 「破了之後走多遠」—— 那是訊號層。

### 指標現況

`strategies/research/S16_MACrossShort/indicators/IND_S16S_P29.pla`

兩層並存，互不覆蓋：

| | SECTION 7（既有） | SECTION 7b（新增） |
|---|---|---|
| 破的基準 | 斜線延伸 | 型態自身水平極值線 |
| 承載裁示 | 14、18 | **C** |
| 回答 | 型態何時不再存活 | 型態從哪一邊離開 |

八個角色八種顏色：上緣線 Red／下緣線 LightGray／state1 Blue／state2 Magenta／
P49 本體 Yellow／P50 本體 Cyan／**跌破 White**／**突破 Green**。

---

## 4. Decisions（今天的裁示與判斷）

| 編號 | 題目 | 裁示 |
|---|---|---|
| **A1** | P51／P52 定義 | 維持，**角色 ＝ 屬性不是濾網**（218／年，不可能當進場濾網） |
| **B1** | P53／P54 的「水平」 | **維持完全相等**，不引進容差（八年 15 個，四年掛零） |
| **C** | 破的判準 | **收盤價 ＋ 型態自身水平極值線**，無緩衝、無 N 根確認 |
| 顯示 | 標籤 | 成立標 `P49 #12`，破位標 `#12 DN`／`#12 UP`，**共用序號可對回** |
| 顯示 | 跌破 K 棒 | **純白**（用戶裁示）；突破 **Green**（黑底，不可用深色） |

### ★ 260841 是今天最重要的修正

`v_H3` / `v_L3` / `v_H3b` / `v_L3b` 在樞紐鏈滿六個後**每來一個新樞紐就被覆寫**，
但 `v_SlopeUp` / `v_SlopeDn` 只在型態成立時重算。於是：

```
v_UpLine = v_H3 + v_SlopeUp * ( CurrentBar - v_H3b );
             ↑ 後來某個樞紐      ↑ 這個型態的斜率
```

**203/203 全中，中位數在成立後第 3 根就壞掉。**
圖上每條線過了三根都是亂的，而且 SECTION 7 的判破也一直在讀它。
**從 260841 起 SECTION 7 才第一次跑在正確的線上。**

（裁示 14／18 本身沒問題 —— 那是在 Python 上決定的，錯的是 .pla 的實作。）

### 未裁示（不急）

| | 選項 | 影響 |
|---|---|---|
| **F1** | 維持 E1（線走後兩樞紐） | 現況，1% 的線會跑很遠（最大 4.6x 型態高度） |
| **F2** | 改成 E2（線走首尾樞紐） | 最大降到 2.0x，但**會改變 SECTION 7 的 state 轉換行為**，不是純美化 |

---

## 5. Next（明天第一件事）

### ★ 唯一未結：199 vs 203

| 項目 | 基準 | MC12 | |
|---|---|---|---|
| formations | 203 | **199** | −4 |
| P49 | 113 | **111** | −2 |
| P50 | 90 | **88** | −2 |
| break_down | 76 | 75 | −1 |
| break_up | 88 | 85 | −3 |
| expired | 39 | 39 | ✅ |
| slot_overflow | 0 | 0 | ✅ |

**追蹤器本身是對的**：75+85+39 = 199 ＝ 它看到的型態數，P49+P50 = 199 也吻合，
`slot_overflow = 0`。沒有型態卡在槽裡。**純粹是偵測端少看到 4 個。**

**已排除四個假設：**

| 假設 | 排除依據 |
|---|---|
| 暖機吃掉開頭 | 最早的參考型態在第 6,140 根才確認，遠超 MaxBarsBack 100 |
| 外包棒推入順序 | `for v_k = 1 to 2` 先推 H 後推 L，與 Python 同索引排序一致 |
| 時段閘門 | `v_BIS >= 3` 在樞紐下一根檢查，等價於 `S[i]>=2 且 S[i+1]>=3` |
| M7 方向判別 | P49／P50 各少 2 個平均分布；最小 \|M7\| = 0.005，無型態逼近分界 |

**剩下的嫌疑：資料本身。** 參考 CSV 是從 1 分鐘匯出檔聚合成 5 分鐘的（見 `f92cd4f`），
MC 自己的 5 分鐘 K 棒不保證聚合方式一致 —— 時段邊界、無成交 K 棒、跳空處都可能差。

### 操作步驟（四步，缺一不可）

1. **重新 Import 或在 PowerLanguage Editor 重新編譯 .pla** ——
   MC 編譯進自己的資料庫，**不讀磁碟**，更新檔案不會自動生效
2. **把圖上的指標移除再重新掛上** —— MC 保留舊 study 實例的 input 值
3. **確認第一根印出 `IND_S16S_P29 build 260843`** —— 看到 260843 才算數。
   建議 `Print_Events` 設 0，輸出視窗只剩六行
4. **座標 → 座標範圍 → 和商品一致** ——
   不設的話 `PlotPaintBar` 會跨 `NoPlot` 缺口連線，畫出橫跨整張圖的假線

### 要回傳的六行

```
P29 DIAG   bars=??????  first=???????? ????  last=???????? ????
P29 DIAG   pivot_high=??????  pivot_low=??????
P29 DIAG   by_year 2019..2026 =   ?   ?   ?   ?   ?   ?   ?   ?
P29 DIAG   reference       =    9  15  18  31  24  31  42  33
P29 TALLY  formations=?????  P49=?????  P50=?????
P29 TALLY  break_down=?????  break_up=?????  expired=?????  slot_overflow=?????
```

**對照基準**（量自 `scripts/research/s16s_5min.csv`）：

| DIAG 欄位 | 應為 |
|---|---|
| bars | **421,513** |
| first | **20190102 0850** |
| last | **20260822 0500** |
| pivot_high | **75,127** |
| pivot_low | **76,020** |
| by_year | **9 · 15 · 18 · 31 · 24 · 31 · 42 · 33** |

**判讀**：bars 或日期對不上 → 圖表載入範圍問題；bars 對但樞紐數不同 → 資料內容有差；
by_year 集中在某一年 → 那年的資料有問題；全對才輪到程式邏輯。

### 六行對上之後

1. 把 P53／P54 加進指標（成本近乎零，覆蓋 0.03% 的 K 棒）
2. 回到型態層剩下 32 種 —— 先確認**是否砍掉 D 組 4 種**
   （P07 布林擠壓／P69 艾略特／P70 費波那契／P71 江恩），砍掉剩 28 種
3. 建議排序第 1 順位：**P31 島狀反轉／P62 島狀底／P67 缺口四分類**
   —— 缺口是離散事件，零參數最乾淨，且**三者共用同一套缺口偵測**

---

## 6. Files

### 文件

| 檔案 | 內容 |
|---|---|
| `docs/research/S16S_BROADENING_FAMILY_CLOSED_20260827.md` | 擴散家族結案，五段格式，含永久結論 |
| `docs/research/S16S_pattern_layer_inventory.md` | 型態層剩餘 32 種，分 A/B/C/D 四組 ＋ 建議排序 |
| `docs/research/S16S_ARTIFACTS.md` | 所有 HTML 頁面網址索引 |

### HTML（**筆電上先開這個**）

**https://claude.ai/code/artifact/62ab6ce8-09e2-4deb-a7f3-784357258278**

九個面板：裁示結果／四變體形狀／真實案例／為什麼 P29 稀有／斜線陷阱／
破位結果與永久結論／**指標實作狀態**／**★ 199 vs 203 與明天的四步驟**／逐年分布。

### 腳本（全部無損益欄）

```
scripts/research/s16s_p51_p54_scan.py          四變體母體、容差、逐年
scripts/research/s16s_p51_p54_pick.py          示意圖真實案例（中位振幅）
scripts/research/s16s_p51_p54_make_diagram.py  產生上面那頁
scripts/research/s16s_frame_decompose.py       32,094 框架 3x3 交叉表
scripts/research/s16s_break_outcome.py         破位四種定義
scripts/research/s16s_p29_break_split.py       P29 子分類 × 破位，排列檢定
scripts/research/_diagram.css                  P29 與 P51-P54 兩頁共用
scripts/verify_pla_build_id.py                 檔頭 Build_ID vs input（新）
```

### 已知待辦（非阻塞）

- `verify_pla_semantics.py` **寫入偵測有盲點** —— 認不出與 `if...then` 同一行的指派。
  全 repo 有 **148 處**，含現行 S16_S v1.26.0 的 15 處。已開背景任務。
- B-1：L1 回測不可重現（500/501 筆）
- B-2：四支 live 策略未過 Rule #13 MDD；L3 的 95% MDD 達帳戶 106.3%

---

## 7. Git

```
89962fe  Build 260843  逐年直方圖
71b1d89  Build_ID 檔頭一致性檢查器 + 修兩處飄移
d6de7f9  Build 260842  平移補 PvDt/PvTm + DIAG
78883cc  檔頭同步 260841 / 203
fcd1f75  Build 260841  ★ 錨點鎖存 + 八色配色
c4de618  結案文件補完
2695ad8  Build 260840  破位追蹤 + 白K + 標籤
9e2abff  P51-P54 頁面重建為結案版
1fc221d  擴散家族結案 + 型態層剩餘清單
d452e9c  破位研究（距離不是型態）
38288c1  P51-P54 示意圖
```

Remote：`https://github.com/DRwillychiu/TXF1-Strategy-Lab.git`　branch `main`

**筆電上第一件事：`git pull`。**
