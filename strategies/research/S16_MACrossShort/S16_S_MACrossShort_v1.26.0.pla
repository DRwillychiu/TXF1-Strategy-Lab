{ ================================================================================
  S16_S - MOVING AVERAGE CROSS SHORT STRATEGY
  Signal Name : S16_S_MACrossShort
  MC Load Name: STRATEGY_GEN_S16_S_MACrossShort
  Version     : v1.26.0 K-bar Admission (2026-08-17, base v1.25.0)

  ADOPTED 2026-08-19. This file now SHIPS WITH THE FILTER ON.

    KB_Block_Support 1 / KB_Block_Oppose 1 / KB_Block_Neutral 0
    KB_Filter_Mode 2 / KB_Filter_Win 3

    anchor   160 trades / 2,796,800 / PF 2.3295303290 / -337,200
             adjusted PF 1.8282191963 / sharpe 0.2150697367
             sortino 1.4236697865 / annual return 18.4562%

  2026-08-21 -- THE TWO 2026-08-20 FIXES ARE MEASURED. ANCHOR UNCHANGED.

  A 4-cell sweep settled both, and the attribution is exact.

    Sess_GapReset  ReEntry  net        trades  gross loss   drawdown
       0              0     2,796,800    160   -2,103,600   -337,200
       1              0     2,796,800    160   -2,103,600   -337,200   ADOPTED
       0              1     2,769,200    161   -2,131,200   -364,800
       1              1     2,769,200    161   -2,131,200   -364,800

  Sess_GapReset_On is FREE. Rows sharing a ReEntry value are bit-identical,
  so it changed not one trade in sample. It still ships ON -- the 240-bar
  phantom day session was measured and is real; this sample simply held no
  entry decision on an affected bar. Insurance at zero cost.

  ReEntry_Filt_Mode carries the ENTIRE difference, and it is one trade.
  Gross profit is identical at 4,900,400 in all four cells; gross loss moves
  by exactly 27,600 and the drawdown deepens by exactly 27,600. Setting it to
  1 opens one extra re-entry, that trade loses 27,600, and it lands inside
  the worst stretch.

  USER RULING 2026-08-21: ReEntry_Filt_Mode = 0. Explicitly NOT because it
  is better in sample -- that is n = 1 and would be a bad reason. Because:

    the re-entry path's filter rule has never been swept EITHER way, so
    Mode 2 / Win 3 exists only as a death-cross measurement and pushing it
    onto re-entry extrapolates a result off the path it was measured on;

    the two paths are not the same event -- a fresh death cross at market
    versus a stop order at a price that already failed once -- so demanding
    the entry bar itself be clean is a coherent higher bar for a retry,
    independent of any number;

    re-entry is 7 to 8 trades and can never be measured to significance, so
    this was always going to be a design decision. Better to say so than to
    dress it as an optimisation.

    Sess_GapReset_On   v_Bars_In_Sess could not see a session boundary when
                       an entire session was missing from the data. Measured:
                       20210901 0850 to 20210906 1345 counted as one 240-bar
                       "day session". The K-bar session gate then over-permits
                       -- a 3 or 5 bar pattern built across a boundary nobody
                       detected. 131 of 3,670 sessions are abnormal, of which
                       91 are settlement days ending 1330 and legitimate.

    ReEntry_Filt_Mode  the two entry paths were using DIFFERENT K-bar rules.
                       The death cross gate reads v_Filt_Pass, which at Win = 3
                       accepts a clean bar up to three back. Re-entry read
                       v_KB_Block, demanding the entry bar itself be clean --
                       stricter than the path the sweep measured. Not a
                       decision; the flag was added after the death cross gate
                       had already moved to v_Filt_Pass.

  To reproduce v1.25.0 from this file, set the three block switches AND
  KB_Filter_Mode AND KB_Filter_Win all to 0. That returns 180 trades /
  2,615,600 / PF 2.0923822252 / -364,800 -- verified in MC12 on 2026-08-18.
  The two 2026-08-20 switches do not need to be zeroed for that: with
  KB_Filter_Win 0 the re-entry mode collapses, but Sess_GapReset_On still
  bites, so zero it as well for a bit-exact v1.25.0.

  Any edit must reproduce one of those sets before its own result is read.
  MC12 retains the previous run's input values rather than reloading the
  file, and that has voided whole batches in this project.

  STILL OPEN, ranked: the K-bar filter's +181,200 was selected and measured
  on the SAME 180 trades, so it is not out-of-sample evidence. A permutation
  test, a transfer to S16_S_10M or L2_TrendShort without retuning, and W4
  WFA with PER-WINDOW re-derivation are the three things that would test it.
  None has been run.

  ================================================================
  WHY THIS CELL, AND WHAT IT COST
  ================================================================

  The comparison is unusually clean: the 160 trades are a strict SUBSET of
  v1.25.0's 180, no trade was added, and every surviving trade has an
  IDENTICAL P&L. So the entire +181,200 is exactly the removal of 20
  trades, with nothing else moving.

  Those 20 were worth -181,200 at a 20% win rate and PF 0.38, and 15 of
  them died to the 4-bar quick stop. NONE of them was a big fish -- all
  seven survive, and the top-7 total is 2,384,800 in both versions. The
  filter did not touch the tail.

  It was not free. It also removed a +80,800 structure exit on 2025-04-09
  and a +20,800 golden cross on 2020-09-21. 2025 is the only year that
  gets worse, and that trade is why.

  THE WINDOW IS WHAT MAKES IT AFFORDABLE

  Blocking at the entry bar -- Mode 2 with Win 0, or equivalently Mode 0
  with the switches on -- leaves 102 trades, 1.31 a month, well under the
  floor. Requiring only that a CLEAN bar existed within the last three
  keeps 160. Win is a quality-for-quantity slider and both curves are
  monotone:

    Win     0      1      2      3      4      5      6
    trades  102    127    149    160    167    170    174
    PF     3.29   2.54   2.45   2.33   2.19   2.18   2.13
    net    2.657  2.447  2.760  2.797  2.661  2.668  2.612  (millions)

  Net peaks at 3 because that is where the two curves cross. Win 3, 4, 5
  and 6 all land between 2.61M and 2.80M with an IDENTICAL -337,200
  drawdown -- four of seven values, so the plateau is 57% of the swept
  range against a 20% requirement. Win 0, 1 and 2 fail the trade floor.

  Win 5 is the alternative if the trade-count margin matters: 170 trades
  and 14 of headroom instead of 4, costing 129,200. It was rejected
  because the drawdown is flat across the whole plateau, so those ten
  extra trades buy no protection -- they only dilute.

  NO REPLACEMENT EFFECT. AT ALL.

  Earlier notes in this file assumed MC12 would refill a blocked slot with
  a later cross, making every offline count a FLOOR. It does not. Zero
  trades were added at any Win value; the count moves only as the block
  loosens. A blocked death cross does not free the position for another
  one, because the next cross needs a golden cross in between. Offline
  counts are the actual counts.

  ================================================================
  THE SELECTOR: 28 PATTERNS, ONE INTEGER, NO WEIGHTS
  ================================================================

  Every A, B and C class pattern is computed here -- 13 pro-short, 13
  anti-short, 2 neutral. They do not vote and they do not score. The
  first one that matches writes its own number into v_KB_Code and every
  later test is skipped. The gate is one comparison: v_KB_Code = 0.

  A SCORE WOULD HAVE NOTHING TO FIT. Measured on the 180 v1.25.0 trades,
  the number of patterns an entry carries does not predict anything:

    patterns hit    trades    average    win%     PF
        0              95     26,332     35.8    3.10
        1              65        412     21.5    1.03
        2              14      5,171     35.7    1.44
        3               6      2,466     33.3    1.23

  Hitting three is no worse than hitting one; two is better than one.
  There is no dose-response, so weights would be fitted to noise, and
  every weight is a free parameter this file would then have to defend.
  The signal is binary -- a pattern is present, or none is -- and that
  is exactly what one integer and one comparison express.

  Because the gate only asks whether the code is zero, the ORDER of the
  tests changes the diagnostic label and nothing else. No ordering
  argument is needed, and none was made.

  WHY ALL 28 AND NOT THE 14 THAT CAN FIRE

  The entry gate forces a black bar, so any pattern ending in a white
  one is structurally unreachable and 14 of the 28 must report zero.
  They are coded anyway. The offline census claims anti-short patterns
  never appear on an entry bar; with those 14 present, MC12 tests that
  claim itself instead of taking the Python census on trust -- which
  matters, because section 2.1 of the spec has that census pipeline
  under an open audit. A pattern that fires when it was predicted not
  to is a finding, and there is no way to see it if it is not coded.

  FILTER ORDER: TWO OF THE THREE OPTIONS ARE THE SAME OPTION

  v_Slope_Pass and v_KB_Block are both pure functions of the current bar
  and neither reads the other -- they meet only in the entry gate, ANDed.
  AND commutes, so "slope first then pattern" and "pattern first then
  slope" select the IDENTICAL set of bars. That is provable, not
  arguable, and no backtest can separate them.

  Order can only matter if a condition is allowed to persist. So each
  gets an age -- 0 means it holds on this bar, n means it last held n
  bars ago, 999 means not since the session opened -- and KB_Filter_Mode
  chooses which one is allowed to be stale:

    0  both must hold on this bar               v1.25.0 exactly
    1  slope may be up to Win bars old          slope is the first filter
    2  a clean bar arms for Win bars            pattern is the first filter
    3  either arms, the other confirms          independent, first to fire

  KB_Filter_Win = 0 pins both ages to 0, so all four modes collapse to
  the same-bar AND and the anchor cannot drift no matter what mode is
  left set.

  RE-ENTRY NOW CARRIES THE PATTERN GATE TOO

  SE_MA_ReEntry checked every other block flag but not v_KB_Block, so 7
  trades worth 152,400 -- 3.9% of trades and 5.8% of net profit -- ran
  outside the filter being measured. Not a leak of blocked signals, since
  a blocked entry never opens a position to re-enter, but it would have
  diluted every cell of the sweep. Fixed.

  THE TWO QUICKSTOP LEGS NOW HAVE THEIR OWN SWITCHES

  QuickStop_On turned both legs off together, so "run without the time
  quick-stop but keep the loss stop" was not expressible and had to be
  faked by pushing MaxBars past the hold cap. QS_Time_On and QS_Loss_On
  are explicit, default to 1, and reproduce the old behaviour exactly.

  Both legs are still market orders on a price condition. That is fine
  for the TIME leg, which is a bar-count rule -- but for the LOSS leg it
  is a known defect: the threshold is breached at a bar close and the
  fill lands at the next open, wherever that is. It stays harmless only
  while QS_MaxLoss_Pct is unreachable at 0.70.

  ** ANY SWEEP CELL THAT ACTUALLY FIRES THE LOSS LEG IS MEASURING THAT
  DEFECT. ** Those numbers are informative about the mechanism and are
  NOT a deployment candidate. Fixing it means placing a resting stop
  every bar, which changes what preempts what in the priority chain --
  a separate design question, not a parameter.

  WHAT BLOCKING COSTS

    block                        trades  /month     net      PF
    nothing                         180    2.31  2,615,600  2.09
    pro-short only                  105    1.35  2,581,600  3.06
    pro-short + neutral              95    1.22  2,501,600  3.10

  Those counts assume a blocked slot stays empty. MC12 will REFILL --
  blocking a trade frees the position for a later cross that was
  previously skipped -- so each count is a FLOOR, and how far above it
  the real number lands is the one thing this sweep exists to find out.
  Every option shown already breaks the 156-trade floor on arithmetic.

  Note the last row: net profit FALLS while PF rises. A18 tweezer top,
  B29 three outside down and A10 outside bar are profitable and go out
  with the rest. Quality and size separate here, so "maximise PF" and
  "maximise net" are different instructions.

  THRESHOLDS ARE PINNED, NOT TUNED

    long body   >= 50% of range        body exceeds shadow
    doji        <  10% of range, range >= 3 pts, both shadows >= 1 pt
    equal       EXACTLY equal, zero tolerance
    Eight patterns times three knobs would be enough free parameters to
    manufacture significance on 7,775 crosses. The doji threshold is
    relative because the absolute form drifts: median range went 4 points
    in 2019 to 48 in 2026, so "body <= 1 point" silently tightens from
    25% of range to 2%, and doji frequency falls 15.4% -> 4.6% on nothing
    but definitional creep.

  WHY THESE EIGHT AND NOT MORE

    Seventeen gap-dependent patterns were deleted. Not because intraday
    bars never gap -- 14.65% of them open outside the prior body -- but
    because the median of those gaps is ONE POINT against 17 at the
    session open. Dark Cloud Cover means "opened higher then got pushed
    back"; opening one point higher and closing black is just a black
    bar. The condition would be met by quote noise 60,716 times.

    Six further patterns are defined but too rare to test (three black
    crows 30, advance block 14, deliberation 11, three white soldiers 3,
    three stars in the south 0, unique three river bottom 0). Three black
    crows stays in this file at group 5 because the author ruled that
    strictness beats sample size: its strict form appears 30 times and
    its loose form 1,237, and the loose form's "opens inside the prior
    body" clause is a daily-chart artifact that on 5-min bars amounts to
    demanding a gap.

  PRE-REGISTERED: GROUP 11 IS THE HYPOTHESIS

    Groups 1-10 are diagnostic. Reading eleven cells and adopting the
    best is selection, not evidence. Acceptance needs all three: the
    added trades must be profitable ON THEIR OWN, the original 180 must
    survive at 170 or better, and the drawdown must not worsen.

  FULL DESIGN RECORD AND VERSION HISTORY
      docs/research/S16S_design_record.md

  Nothing was deleted when this file was slimmed on 2026-08-15. Every
  block that used to live here is in that document, and each divider
  below carries the section number that explains it. The move was
  verified by stripping all comments from the before and after files
  and asserting the remaining code was byte-identical.

  CURRENT ANCHOR  (MC12, every input at the default in this file)
      net profit                2,615,600
      trades                          180
      profit factor                  2.09
      max strategy drawdown      -364,800
      return on account            908.19

  Any edit must reproduce those five numbers before its own result is
  read. MC12 keeps the previous run's input values instead of reloading
  the file, and that has voided whole batches in this project.

  COST SPEC  (CLAUDE.md)
      slippage 1,000 NTD per contract per side = 10 points round trip
      2 lots, 2,000,000 initial capital, MaxBarsBack 100
      day 0845-1345, night 1500-0500

  RULE COMPLIANCE
      #11 Settlement_Flat, 7 elements
      #12 SetStopContract + SetStopLoss + SL_Pct
      #15 100 percent ASCII
      #17 multi-layer stop loss

  SEMANTIC AUDIT  scripts/verify_pla_semantics.py -- 22 checks, all pass
      Four dead lines removed 2026-08-15 (v_ML_Loss, v_Prev_Open,
      v_Prev_Close, v_Prev_Body). Details in the design record.
================================================================================ }

[IntrabarOrderGeneration = False]
inputs:
    { ===== Group 0: BUILD STAMP -- declared FIRST so it is the first row in ===== }
    { the MC12 settings dialog. The strategy NAME does not change when the file }
    { is patched, so before this there was no way to tell from the dialog which }
    { build MC had loaded. MC12 retains input values rather than reloading the  }
    { file, and that has voided six batches in this project.                    }
    {                                                                           }
    { Format YYMMDD of the last behaviour-changing edit. Compare against        }
    { docs/build_snapshots/ before trusting any run. Generated and checked by   }
    { scripts/snapshot_pla_inputs.py.                                           }
    Build_ID                ( 260820 ),

    { ===== Group A: Entry (ZLEMA) - v0.5-FINAL 2026-07-10 ===== }
    ZLEMA_Fast              ( 25    ),
    ZLEMA_Slow              ( 70    ),
    MinSlope                ( 28    ),

    { ===== Group B: M5 Quick Stop - v0.5, v1.7.1 QS_Loss -> Pct ===== }
    QuickStop_On            ( 1     ),
    QS_Time_On              ( 1     ),     { 1 = time leg live. 0 = off, loss leg unaffected  }
    QS_Loss_On              ( 1     ),     { 1 = loss leg live. 0 = off, time leg unaffected  }
    QuickStop_MaxBars       ( 4     ),
    QS_MaxLoss_Pct          ( 0.70  ),

    { Group C: M6 Rule #17 Multi-Layer Monitor  -- md 3.1 }
    ML_On                   ( 0     ),
    ML_ActivationPct        ( 20    ),
    ML_ScoreTrigger         ( 60    ),
    ML_MinCategories        ( 3     ),
    ML_VolAvgLen            ( 15    ),
    ML_VolSpikeMult         ( 2.0   ),
    ML_MA_ShortLen          ( 10    ),
    ML_ATR_ShortLen         ( 3     ),
    ML_ATR_LongLen          ( 90    ),
    ML_ATR_Ratio            ( 2.5   ),

    { Group D: M7 True Breakeven - v1.11.0 redesign  -- md 3.2 }
    BE_On                   ( 1     ),
    BE_Trigger_Pct          ( 0.70  ),
    BE_Cost_Pts             ( 20    ),

    { Group D2: P3b Profit Trail - v1.12.0  -- md 3.3 }
    Trail_On                ( 0     ),
    { v1.15.0 C2: collapsed. The old 1.20/80 base band was measured dead  -- md 3.4 }
    Trail_Trigger_Pct       ( 2.00  ),
    Trail_Giveback_Pct      ( 40    ),

    { Group D3: A2 Structure Trail - v1.13.0 (ADOPTED)  -- md 3.5 }
    Struct_On               ( 1     ),
    Struct_Lookback         ( 20    ),
    Struct_Buffer_Pts       ( 12    ),
    Struct_UseClose         ( 1     ),
    { v1.15.0 C1: 1.20 -> 1.00. 11-cell sweep, plateau centre of  -- md 3.6 }
    Struct_Trigger_Pct      ( 0.60  ),

    { Group D4: B1 Tiered Giveback - v1.14.0 (DEFAULT OFF)  -- md 3.7 }
    Tier2_On                ( 0     ),
    Tier2_Trigger_Pct       ( 2.00  ),
    Tier2_Giveback_Pct      ( 40    ),

    { Group E: M8 Time Stop - v0.5 (KEY ALPHA DRIVER)  -- md 3.8 }
    M8_On                   ( 1     ),
    MaxHoldingBars          ( 48    ),
    M8_Form                 ( 2     ),     { 1 = bar count (v1.24.0 and earlier). 2 = percentage target + bar backstop. }
    MaxHold_Pct             ( 2.30  ),     { Form 2 only. Cover at EntryPrice - EntryPrice*Pct/100, LIMIT order.        }

    { ===== Group N: K-bar admission - v1.26.0 (SHIPS OFF) ===== }
    KB_Block_Support        ( 1     ),     { ADOPTED 2026-08-19. Block on any of the 13 pro-short patterns  }
    KB_Block_Oppose         ( 1     ),     { ADOPTED 2026-08-19. Block on any of the 13 anti-short patterns }
    KB_Block_Neutral        ( 0     ),     { inside / outside bar stay ALLOWED -- blocking them cost trades  }
    KB_Filter_Mode          ( 2     ),     { ADOPTED 2026-08-19. A clean bar arms for Win bars               }
    KB_Filter_Win           ( 3     ),     { ADOPTED 2026-08-19. Plateau spans 3-6, see the header           }

    { --- 2026-08-20 defect fixes, 2026-08-21 measured and settled by 4 cells --- }
    { Sess_GapReset_On is FREE: cells (0,0) and (1,0) are bit-identical, as are   }
    { (0,1) and (1,1). It closed a real defect and changed no trade in sample.    }
    { ReEntry_Filt_Mode stays 0 by USER RULING -- see the header. The anchor is   }
    { unchanged at 160T / 2,796,800.                                              }
    Sess_GapReset_On        ( 1     ),     { 1 = a wall-clock gap > one bar also starts a session. 0 = old }
    ReEntry_Filt_Mode       ( 0     ),     { 0 = ADOPTED. Re-entry demands a clean bar on the entry bar    }
                                           { itself, stricter than the death cross path. 1 = unify with    }
                                           { v_Filt_Pass, costs one trade at -27,600. Design decision, not }
                                           { an optimisation -- neither value was ever swept on this path. }

    { --- Pattern-selector thresholds. NOTE: these are NOT the KB_Body_* inputs --- }
    { of the legacy 17-type single-bar classifier in SECTION 6. Two separate       }
    { systems, two separate meanings of "long body". Sweeping KB_Body_Long does    }
    { NOT move anything below. User ruling 2026-08-18: exposed as inputs so the    }
    { decision is made on measured P&L, not on reasoning.                          }
    KB_Pat_LongBody         ( 0.50  ),     { long body: body >= this share of range. 0.50 = body >= both shadows }
    KB_Pat_DojiRatio        ( 0.10  ),     { doji: body < this share of range                                   }
    KB_Pat_DojiMinRange     ( 3     ),     { doji: range must be at least this many points                      }
    KB_Pat_DojiMinShadow    ( 1     ),     { doji: each shadow at least this many points                        }
    KB_Pat_MinRange         ( 0     ),     { long body ALSO needs range >= this. 0 = inert, no behaviour change  }

    { ===== Group F: ATR SL (Frozen) - v0.5 + v1.5 SL_Pct ===== }
    ATR_Len                 ( 14    ),
    StopATRMult             ( 4.0   ),
    SL_Pct                  ( 1.00  ),     { Pct stop cap for SHORT. Ceiling = Entry + Entry*SL_Pct/100. Converge=1.00 (MC sweep 0-5.0). }

    { ===== Group H: Rule #11 Compliance ===== }
    { Registry_Valid_Until format: EL-standard YYYMMDD where YYY=Year-1900 }
    { 1270101 = 2027-01-01 (aligned with L1-L5 / S1 / S3_S registry expiry) }
    Holiday_Flat_Time       ( 415   ),
    Registry_Valid_Until    ( 1270101 ),
    Manual_Kill_Switch      ( False ),
    Settlement_Flat_Time    ( 1230  ),

    { Group I: Tail Time Guard - v1.3, rebuilt v1.18.0  -- md 3.9 }
    Tail_NoEntry_From       ( 415   ),
    Tail_ForceExit_Time     ( 430   ),

    { Tail_DayNoEntry_From -- v1.22.0. THE DAY SESSION'S OWN RULE.  -- md 3.10 }
    Tail_DayNoEntry_From    ( 1335  ),

    { Tail_Signal_Lead_Bars -- NOT A TUNING KNOB.  -- md 3.11 }
    Tail_Signal_Lead_Bars   ( 2     ),

    { Group J: Session Gap Filter - v1.16.0 (DEFAULT OFF)  -- md 3.12 }
    Gap_Filter_On           ( 1     ),
    Gap_Day_On              ( 1     ),
    Gap_Night_On            ( 1     ),
    Gap_Down_Min_Pct        ( 0.10  ),
    Gap_Rise_Min_Pct        ( 0.20  ),
    Gap_Scope_Bars          ( 3     ),

    { Group J2: First-Bar Shape Gate - v1.23.0  -- md 3.13 }
    Gap_KB_Gate_On          ( 1     ),
    Gap_KB_Group            ( 1     ),
    Gap_KB_Scope_Bars       ( 3     ),

    { Group J3: Up-Gap Rule - v1.24.0  -- md 3.14 }
    UpGap_Gate_On           ( 0     ),
    UpGap_Mode              ( 1     ),
    UpGap_Min_Pct           ( 0.10  ),
    UpGap_Bar1_Pct          ( 0.20  ),
    UpGap_Scope_Bars        ( 3     ),
    UpGap_Relax_Mult        ( 0.50  ),
    { Session boundaries. Bars are stamped at their CLOSE time, so the  -- md 3.15 }
    Sess_Day_Start          ( 845   ),
    Sess_Day_End            ( 1345  ),
    Sess_Night_Start        ( 1500  ),
    Sess_Night_End          ( 500   ),

    { v1.17.0 ITEM A -- session LAST-bar entry block. ON.  -- md 3.16 }
    Sess_LastBar_Block_On   ( 1     ),
    { Group K: K-bar classification - v1.16.0 (RECORD ONLY)  -- md 3.17 }
    KB_Body_Long            ( 0.75  ),
    KB_Body_Mid             ( 0.30  ),
    KB_Body_Small           ( 0.05  ),
    KB_Shadow_Ratio         ( 2.00  ),

    { Group L: Re-Entry gates - v1.17.0 (ALL DEFAULT ON)  -- md 3.18 }
    ReEntry_On              ( 1     ),
    ReEntry_Structure_Gate  ( 1     ),
    ReEntry_Slope_Gate      ( 1     ),
    ReEntry_Close_Gate      ( 1     ),
    { Group M: Slope form - v1.20.0  -- md 3.19 }
    Slope_Form              ( 2     ),
    { MinSlope_Rate -- the threshold for Slope_Form = 2.  -- md 3.20 }
    MinSlope_Rate           ( 0.050    ),
    { Slope_Require_Adjacent -- the dx fix.  -- md 3.21 }
    Slope_Require_Adjacent  ( 0     );
{ ============================================================
  SECTION 2 - VARIABLES
  ============================================================ }
variables:
    { ===== ZLEMA state ===== }
    v_Fast_Lag              ( 0     ),
    v_Slow_Lag              ( 0     ),
    v_ZLEMA_Fast            ( 0     ),
    v_ZLEMA_Slow            ( 0     ),
    v_Slope                 ( 0     ),

    { ===== Signal detection ===== }
    v_Death_Cross           ( False ),
    v_Golden_Cross          ( False ),

    { ===== ATR ===== }
    v_ATR                   ( 0     ),

    { ===== Trade state ===== }
    v_EntryBar              ( 0     ),
    v_BarsSince             ( 0     ),
    v_Prev_MP               ( 0     ),
    v_Loss                  ( 0     ),
    v_Profit                ( 0     ),

    { ===== Frozen SL (Rule #12) ===== }
    v_SL_Locked             ( False ),
    v_Frozen_ATR            ( 0     ),
    v_Original_Frozen_ATR   ( 0     ),
    v_Frozen_SL_Dist        ( 0     ),
    v_SL_Level              ( 0     ),

    { ===== v1.5 SetStopContract + SL_Pct ===== }
    v_Guard_Distance        ( 0     ),
    v_SL_Pct_Ceil           ( 0     ),

    { ===== M5 Quick Stop state ===== }
    v_QuickStop_Fired       ( False ),
    v_QS_Time_On            ( False ),
    v_QS_Loss_On            ( False ),

    { ===== M8 percentage target (v1.25.0 Form 2) ===== }
    v_M8_Target             ( 0     ),

    { ===== K-bar admission state (v1.26.0) ===== }
    v_KB_Sup                ( False ),
    v_KB_Opp                ( False ),
    v_KB_Neu                ( False ),
    v_KB_On                 ( False ),
    v_KB_Block              ( False ),
    v_KB_Code               ( 0     ),
    v_Slope_Age             ( 999   ),
    v_Clean_Age             ( 999   ),
    v_Filt_Pass             ( False ),
    v_Sess_GapMin           ( 0     ),
    v_Sess_Gap_Seen         ( False ),
    v_ReEntry_Filt_OK       ( False ),
    v_KB_R0                 ( 0     ),
    v_KB_R1                 ( 0     ),
    v_KB_R2                 ( 0     ),
    v_KB_R3                 ( 0     ),
    v_KB_R4                 ( 0     ),
    v_KB_B0                 ( 0     ),
    v_KB_B1                 ( 0     ),
    v_KB_B2                 ( 0     ),
    v_KB_B3                 ( 0     ),
    v_KB_B4                 ( 0     ),
    v_KB_T0                 ( 0     ),
    v_KB_T1                 ( 0     ),
    v_KB_T2                 ( 0     ),
    v_KB_M0                 ( 0     ),
    v_KB_M1                 ( 0     ),
    v_KB_M2                 ( 0     ),
    v_KB_S0                 ( 0     ),
    v_KB_S1                 ( 0     ),
    v_KB_S2                 ( 0     ),
    v_KB_N2                 ( 0     ),
    v_KB_L0                 ( False ),
    v_KB_L1                 ( False ),
    v_KB_L2                 ( False ),
    v_KB_L3                 ( False ),
    v_KB_L4                 ( False ),
    v_KB_D0                 ( False ),

    { ===== M6 Multi-Layer state ===== }
    v_ML_Activation_Level   ( 0     ),
    v_ML_Score              ( 0     ),
    v_ML_Score_Pct          ( 0     ),
    v_ML_CatCount           ( 0     ),
    v_ML_Trigger            ( False ),
    v_ML_Cat_1F             ( 0     ),
    v_ML_Cat_2F             ( 0     ),
    v_ML_Cat_3F             ( 0     ),
    v_ML_Cat_4F             ( 0     ),
    v_ML_Cat_5F             ( 0     ),
    v_ML_AvgVol             ( 0     ),
    v_ML_ATR_Short          ( 0     ),
    v_ML_ATR_Long           ( 0     ),
    v_ML_RSI                ( 0     ),
    v_ML_RSI_Prior          ( 0     ),
    v_ML_MA_Short           ( 0     ),

    { ===== M7 True Breakeven state (v1.11.0) ===== }
    v_BE_Active             ( False ),
    v_BE_Stop_Level         ( 0     ),

    { ===== P3b Profit Trail state (v1.12.0) ===== }
    v_MaxProfit             ( 0     ),
    v_Trail_Active          ( False ),
    v_Trail_Lock            ( 0     ),
    v_Protect_Lock          ( 0     ),
    v_Trail_Binding         ( False ),
    v_Struct_Active         ( False ),
    v_Struct_Window         ( 0     ),
    v_Struct_Ref            ( 0     ),
    v_Struct_Raw            ( 0     ),
    v_Struct_Lock           ( 0     ),
    v_Struct_Binding        ( False ),
    v_Trail_Giveback        ( 0     ),

    { ===== Re-Entry state (v1.6) ===== }
    v_ReEntry_Armed         ( False ),
    v_ReEntry_Price         ( 0     ),
    v_Last_EntryPrice       ( 0     ),
    v_IsReEntry             ( False ),

    { ===== Priority 0 protection state ===== }
    v_Holiday_Block         ( False ),
    v_Registry_Expired      ( False ),
    v_Settlement_Day        ( False ),
    hidx                    ( 0     ),
    ExitFired               ( 0     ),

    { ===== v1.16.0 session boundary state ===== }
    v_In_Day                ( False ),
    v_In_Night              ( False ),
    v_Day_First             ( False ),
    v_Night_First           ( False ),
    v_Sess_First            ( False ),
    v_Bars_In_Sess          ( 0     ),

    { ===== v1.17.0 session LAST bar (fill-side gap guard) ===== }
    v_Sess_LastBar          ( False ),
    v_Sess_LastBar_Block    ( False ),
    v_Tail_Entry_OK         ( True  ),

    { ===== v1.18.0 derived tail entry cutoff ===== }
    v_Tail_Cutoff           ( 0     ),
    v_Day_Cutoff            ( 0     ),

    { ===== v1.19.1 slope as dy/dx ===== }
    v_Slope_Rate            ( 0     ),
    v_Elapsed_Min           ( 0     ),
    v_Slope_Adjacent        ( False ),
    v_Slope_Pass            ( False ),
    v_FirstBar_Block        ( False ),

    { ===== v1.16.0 gap measurement (latched on the session first bar) ===== }
    v_Sess_Gap_Pts          ( 0     ),
    v_Sess_Gap_Pct          ( 0     ),

    { ===== v1.16.0 session first bar OHLC (latched) ===== }
    v_First_Open            ( 0     ),
    v_First_High            ( 0     ),
    v_First_Low             ( 0     ),
    v_First_Close           ( 0     ),
    v_First_Rise_Pct        ( 0     ),

    { ===== v1.16.0 K-bar classification (record only) ===== }
    v_KB_Range              ( 0     ),
    v_KB_Body               ( 0     ),
    v_KB_UpSh               ( 0     ),
    v_KB_DnSh               ( 0     ),
    v_KB_Up                 ( False ),
    v_KB_Type               ( 0     ),

    { ===== v1.21.0 decoded switches =====                       }
    { One boolean mirror per numeric switch input. The code below }
    { still reads as boolean logic; only the input type changed.  }
    v_QuickStop_On          ( False ),
    v_ML_On                 ( False ),
    v_BE_On                 ( False ),
    v_Trail_On              ( False ),
    v_Struct_On             ( False ),
    v_Struct_UseClose       ( False ),
    v_Tier2_On              ( False ),
    v_M8_On                 ( False ),
    v_Gap_Filter_On         ( False ),
    v_Gap_Day_On            ( False ),
    v_Gap_Night_On          ( False ),
    v_Sess_LastBar_Block_On ( False ),
    v_ReEntry_On            ( False ),
    v_ReEntry_Structure_Gate ( False ),
    v_ReEntry_Slope_Gate    ( False ),
    v_ReEntry_Close_Gate    ( False ),
    v_Slope_Require_Adjacent ( False ),

    { ===== v1.23.0 first-bar shape gate ===== }
    v_Gap_KB_Gate_On        ( False ),
    v_KB_InGroup            ( False ),

    { ===== v1.24.0 up-gap rule ===== }
    v_UpGap_Gate_On         ( False ),
    v_UpGap_Relax           ( False ),
    v_MinSlope_Eff          ( 0     ),

    { ===== v1.16.0 gap filter verdict ===== }
    v_Gap_Block             ( False );

arrays:
    Holiday_Tail[80](0);


    { SECTION 2.4 - BUILD STAMP  -- one line per run, into the output log }
    { A second, independent record of which build actually ran. The dialog     }
    { shows Build_ID as its first row; this puts the same number in the print  }
    { log, so a saved report can be traced back to a file even months later.   }
if CurrentBar = 1 then
    Print( "S16_S build ", Build_ID );


    { SECTION 2.5 - SWITCH DECODE  -- md 3.22 }

v_QuickStop_On          = ( QuickStop_On          <> 0 );
v_QS_Time_On            = ( QS_Time_On            <> 0 );
v_QS_Loss_On            = ( QS_Loss_On            <> 0 );
v_ML_On                 = ( ML_On                 <> 0 );
v_BE_On                 = ( BE_On                 <> 0 );
v_Trail_On              = ( Trail_On              <> 0 );
v_Struct_On             = ( Struct_On             <> 0 );
v_Struct_UseClose       = ( Struct_UseClose       <> 0 );
v_Tier2_On              = ( Tier2_On              <> 0 );
v_M8_On                 = ( M8_On                 <> 0 );
v_KB_Sup                = ( KB_Block_Support      <> 0 );
v_KB_Opp                = ( KB_Block_Oppose       <> 0 );
v_KB_Neu                = ( KB_Block_Neutral      <> 0 );
v_KB_On                 = ( v_KB_Sup or v_KB_Opp or v_KB_Neu );
v_Gap_Filter_On         = ( Gap_Filter_On         <> 0 );
v_Gap_Day_On            = ( Gap_Day_On            <> 0 );
v_Gap_Night_On          = ( Gap_Night_On          <> 0 );
v_Sess_LastBar_Block_On = ( Sess_LastBar_Block_On <> 0 );
v_ReEntry_On            = ( ReEntry_On            <> 0 );
v_ReEntry_Structure_Gate = ( ReEntry_Structure_Gate <> 0 );
v_ReEntry_Slope_Gate    = ( ReEntry_Slope_Gate    <> 0 );
v_ReEntry_Close_Gate    = ( ReEntry_Close_Gate    <> 0 );
v_Slope_Require_Adjacent = ( Slope_Require_Adjacent <> 0 );
v_Gap_KB_Gate_On        = ( Gap_KB_Gate_On        <> 0 );
v_UpGap_Gate_On         = ( UpGap_Gate_On         <> 0 );

    { SECTION 3 - PRIORITY 0 COMPLIANCE CHECKS  -- md 3.23 }
if CurrentBar = 1 then begin
    Holiday_Tail[1]  = 1190913;  Holiday_Tail[2]  = 1191010;
    Holiday_Tail[3]  = 1200101;
    Holiday_Tail[4]  = 1200121;  Holiday_Tail[5]  = 1200228;
    Holiday_Tail[6]  = 1200402;  Holiday_Tail[7]  = 1200501;
    Holiday_Tail[8]  = 1200625;  Holiday_Tail[9]  = 1201001;
    Holiday_Tail[10] = 1201009;  Holiday_Tail[11] = 1210101;
    Holiday_Tail[12] = 1210206;  Holiday_Tail[13] = 1210227;
    Holiday_Tail[14] = 1210402;  Holiday_Tail[15] = 1210612;
    Holiday_Tail[16] = 1210918;  Holiday_Tail[17] = 1211009;
    Holiday_Tail[18] = 1211231;
    Holiday_Tail[19] = 1220127;  Holiday_Tail[20] = 1220226;
    Holiday_Tail[21] = 1220402;  Holiday_Tail[22] = 1220430;
    Holiday_Tail[23] = 1220603;  Holiday_Tail[24] = 1220909;
    Holiday_Tail[25] = 1221008;  Holiday_Tail[26] = 1221231;
    Holiday_Tail[27] = 1230118;  Holiday_Tail[28] = 1230225;
    Holiday_Tail[29] = 1230401;  Holiday_Tail[30] = 1230429;
    Holiday_Tail[31] = 1230622;  Holiday_Tail[32] = 1230929;
    Holiday_Tail[33] = 1231007;  Holiday_Tail[34] = 1231230;
    Holiday_Tail[35] = 1240206;  Holiday_Tail[36] = 1240228;
    Holiday_Tail[37] = 1240404;  Holiday_Tail[38] = 1240501;
    Holiday_Tail[39] = 1240608;  Holiday_Tail[40] = 1240917;
    Holiday_Tail[41] = 1241010;  Holiday_Tail[42] = 1250101;
    Holiday_Tail[43] = 1250123;  Holiday_Tail[44] = 1250228;
    Holiday_Tail[45] = 1250403;  Holiday_Tail[46] = 1250501;
    Holiday_Tail[47] = 1250530;  Holiday_Tail[48] = 1250927;
    Holiday_Tail[49] = 1251004;  Holiday_Tail[50] = 1251010;
    Holiday_Tail[51] = 1251024;  Holiday_Tail[52] = 1251225;
    Holiday_Tail[53] = 1260101;
    Holiday_Tail[54] = 1260212;  Holiday_Tail[55] = 1260227;
    Holiday_Tail[56] = 1260403;  Holiday_Tail[57] = 1260501;
    Holiday_Tail[58] = 1260619;  Holiday_Tail[59] = 1260925;
    Holiday_Tail[60] = 1261009;  Holiday_Tail[61] = 1261024;
    Holiday_Tail[62] = 1261225;  Holiday_Tail[63] = 1270101;
end;

{ ---- Registry expiry check (EL-standard: Date is YYYMMDD Year-1900) ---- }
v_Registry_Expired = False;
if Date > Registry_Valid_Until then
    v_Registry_Expired = True;

{ ---- Holiday match: check current date against registry tail (Time <= 500) ---- }
v_Holiday_Block = False;
if Time <= 500 then begin
    for hidx = 1 to 80 begin
        if Date = Holiday_Tail[hidx] then
            v_Holiday_Block = True;
    end;
end;

{ ---- Registry expiry escalation: force block once expired ---- }
if v_Registry_Expired = True then
    v_Holiday_Block = True;

{ ---- Settlement day: 3rd Wednesday of month ---- }
v_Settlement_Day = ( DayOfWeek(Date) = 3 ) and
                   ( DayOfMonth(Date) >= 15 ) and
                   ( DayOfMonth(Date) <= 21 );


    { SECTION 4 - INDICATORS (ZLEMA)  -- md 3.24 }

v_Fast_Lag = IntPortion( ( ZLEMA_Fast - 1 ) / 2 );
v_Slow_Lag = IntPortion( ( ZLEMA_Slow - 1 ) / 2 );

v_ZLEMA_Fast = XAverage( 2 * Close - Close[v_Fast_Lag], ZLEMA_Fast );
v_ZLEMA_Slow = XAverage( 2 * Close - Close[v_Slow_Lag], ZLEMA_Slow );

{ F2 fix: measure Fast ZLEMA downward slope for short entry (not Slow, not AbsValue) }
{ Positive value = Fast is falling = bearish momentum = good for short }
v_Slope = v_ZLEMA_Fast[1] - v_ZLEMA_Fast;


    { SECTION 4.5 - SESSION BOUNDARY, OPENING GAP, K-BAR TYPE  -- md 3.25 }
v_In_Day   = Time >  Sess_Day_Start   and Time <= Sess_Day_End;
v_In_Night = Time >  Sess_Night_Start or  Time <= Sess_Night_End;

    { ---- First bar of a session ----------------------------------  -- md 3.26 }
    { 2026-08-20 DEFECT FIX. The transition test alone CANNOT see a boundary    }
    { when an entire session is missing from the data. Measured: 20210901 0850  }
    { through 20210906 1345 was counted as ONE 240-bar "day session", because   }
    { the night sessions in between have no bars at all, so v_In_Day never went }
    { False and the counter never reset. The K-bar session gate then over-      }
    { permits: a 3 or 5 bar pattern can reach across a boundary nobody saw.     }
    {                                                                           }
    { A wall-clock gap larger than one bar now also starts a session. Date is   }
    { carried through DateToJulian so a multi-DAY hole is measured, which the   }
    { existing v_Elapsed_Min cannot do -- it wraps at 1440 and would report a   }
    { five-day hole as a few minutes.                                           }
    {                                                                           }
    { Erring toward RESET is the safe direction. A spurious reset only makes    }
    { patterns unavailable for a few bars; a MISSED reset lets a pattern be     }
    { built out of bars from two different markets.                             }
v_Sess_GapMin = 0;
if CurrentBar > 1 then
    v_Sess_GapMin = ( DateToJulian( Date ) - DateToJulian( Date[1] ) ) * 1440
                  + ( TimeToMinutes( Time ) - TimeToMinutes( Time[1] ) );

v_Sess_Gap_Seen = ( Sess_GapReset_On <> 0 ) and ( CurrentBar > 1 )
                  and ( BarInterval > 0 ) and ( v_Sess_GapMin > BarInterval );

v_Day_First   = False;
v_Night_First = False;
if CurrentBar > 1 then begin
    if v_In_Day   and ( v_In_Day[1]   = False or v_Sess_Gap_Seen ) then
        v_Day_First   = True;
    if v_In_Night and ( v_In_Night[1] = False or v_Sess_Gap_Seen ) then
        v_Night_First = True;
end;
v_Sess_First = v_Day_First or v_Night_First;

{ ---- Bar ordinal within the session -------------------------- }
if v_Sess_First then
    v_Bars_In_Sess = 1
else if v_In_Day or v_In_Night then
    v_Bars_In_Sess = v_Bars_In_Sess + 1
else
    v_Bars_In_Sess = 0;

    { ---- Last bar of a session (v1.17.0, ITEM A) -----------------  -- md 3.27 }
v_Sess_LastBar = ( v_In_Day   and Time = Sess_Day_End   ) or
                 ( v_In_Night and Time = Sess_Night_End );

    { ---- Latch the gap and the opening bar OHLC ------------------  -- md 3.28 }
if v_Sess_First then begin

    v_Sess_Gap_Pts = Open - Close[1];
    if Close[1] > 0 then
        v_Sess_Gap_Pct = v_Sess_Gap_Pts / Close[1] * 100
    else
        v_Sess_Gap_Pct = 0;

    v_First_Open  = Open;
    v_First_High  = High;
    v_First_Low   = Low;
    v_First_Close = Close;
    if Open > 0 then
        v_First_Rise_Pct = ( Close - Open ) / Open * 100
    else
        v_First_Rise_Pct = 0;
end;

    { ---- K-bar taxonomy of the session opening bar (RECORD ONLY) --  -- md 3.29 }
if v_Sess_First then begin
    v_KB_Range = v_First_High - v_First_Low;
    v_KB_Up    = v_First_Close > v_First_Open;

    if v_KB_Range <= 0 then begin
        v_KB_Body  = 0;
        v_KB_UpSh  = 0;
        v_KB_DnSh  = 0;
        v_KB_Type  = 0;
    end
    else begin
        v_KB_Body = AbsValue( v_First_Close - v_First_Open ) / v_KB_Range;
        v_KB_UpSh = ( v_First_High - MaxList( v_First_Open, v_First_Close ) ) / v_KB_Range;
        v_KB_DnSh = ( MinList( v_First_Open, v_First_Close ) - v_First_Low ) / v_KB_Range;

        if v_KB_Body >= KB_Body_Long then begin
            if v_KB_Up then v_KB_Type = 1 else v_KB_Type = 2;
        end
        else if v_KB_Body >= KB_Body_Mid then begin
            if v_KB_DnSh >= KB_Shadow_Ratio * v_KB_UpSh then begin
                if v_KB_Up then v_KB_Type = 3 else v_KB_Type = 4;
            end
            else if v_KB_UpSh >= KB_Shadow_Ratio * v_KB_DnSh then begin
                if v_KB_Up then v_KB_Type = 5 else v_KB_Type = 6;
            end
            else begin
                if v_KB_Up then v_KB_Type = 7 else v_KB_Type = 8;
            end;
        end
        else if v_KB_Body >= KB_Body_Small then begin
            if v_KB_DnSh >= KB_Shadow_Ratio * v_KB_UpSh then begin
                if v_KB_Up then v_KB_Type = 9 else v_KB_Type = 10;
            end
            else if v_KB_UpSh >= KB_Shadow_Ratio * v_KB_DnSh then begin
                if v_KB_Up then v_KB_Type = 11 else v_KB_Type = 12;
            end
            else begin
                if v_KB_Up then v_KB_Type = 13 else v_KB_Type = 14;
            end;
        end
        else begin
            if v_KB_DnSh >= KB_Shadow_Ratio * v_KB_UpSh then
                v_KB_Type = 15
            else if v_KB_UpSh >= KB_Shadow_Ratio * v_KB_DnSh then
                v_KB_Type = 16
            else
                v_KB_Type = 17;
        end;
    end;
end;

    { ---- Gap filter verdict --------------------------------------  -- md 3.30 }
v_Gap_Block = False;
if v_Gap_Filter_On and v_Bars_In_Sess >= 1 and v_Bars_In_Sess <= Gap_Scope_Bars then begin
    if ( v_In_Day and v_Gap_Day_On ) or ( v_In_Night and v_Gap_Night_On ) then begin
        if v_Sess_Gap_Pct  <= -Gap_Down_Min_Pct and
           v_First_Rise_Pct >= Gap_Rise_Min_Pct then
            v_Gap_Block = True;
    end;
end;

    { ---- v1.23.0: the first-bar SHAPE gate -----------------------  -- md 3.31 }
if Gap_KB_Group = 2 then
    v_KB_InGroup = ( v_KB_Type = 5  or v_KB_Type = 6  or v_KB_Type = 11 or
                     v_KB_Type = 12 or v_KB_Type = 16 )
else if Gap_KB_Group = 3 then
    v_KB_InGroup = ( v_KB_Type = 7  or v_KB_Type = 8  or v_KB_Type = 13 or
                     v_KB_Type = 14 or v_KB_Type = 17 )
else
    v_KB_InGroup = ( v_KB_Type = 3  or v_KB_Type = 4  or v_KB_Type = 9  or
                     v_KB_Type = 10 or v_KB_Type = 15 );

if v_Gap_KB_Gate_On = True and
   v_Bars_In_Sess >= 1 and v_Bars_In_Sess <= Gap_KB_Scope_Bars and
   v_Sess_Gap_Pct <= -Gap_Down_Min_Pct and
   v_KB_InGroup   =  True then
    v_Gap_Block = True;

    { ---- v1.24.0: the UP-gap rule ---------------------------------  -- md 3.32 }
v_UpGap_Relax = False;
if v_UpGap_Gate_On = True and
   v_Bars_In_Sess  >= 1 and v_Bars_In_Sess <= UpGap_Scope_Bars and
   v_Sess_Gap_Pct  >= UpGap_Min_Pct then begin
    if UpGap_Mode = 2 then begin
        if v_First_Rise_Pct >= UpGap_Bar1_Pct then
            v_Gap_Block = True;
    end
    else begin
        if -v_First_Rise_Pct >= UpGap_Bar1_Pct then
            v_UpGap_Relax = True;
    end;
end;

    { ---- v1.17.0 ITEM A: fill-side twin of the gap filter ---------  -- md 3.33 }
v_Sess_LastBar_Block = False;
if v_Sess_LastBar_Block_On = True and v_Sess_LastBar = True then
    v_Sess_LastBar_Block = True;

    { ---- v1.18.0: DERIVE the signal cutoff from the entry rule -----  -- md 3.34 }
v_Tail_Cutoff = MinutesToTime( TimeToMinutes( Tail_NoEntry_From )
                               - Tail_Signal_Lead_Bars * BarInterval );

    { ---- v1.22.0: the day session's own cutoff ----  -- md 3.35 }
v_Day_Cutoff  = MinutesToTime( TimeToMinutes( Tail_DayNoEntry_From )
                               - Tail_Signal_Lead_Bars * BarInterval );

    { ---- v1.22.0: each session states its own rule ----  -- md 3.36 }
v_Tail_Entry_OK = ( v_In_Day   and Time <= v_Day_Cutoff )
               or ( v_In_Night and ( Time <= v_Tail_Cutoff or Time > 500 ) );


    { SECTION 5 - CROSS DETECTION  -- md 3.37 }

v_Death_Cross  = v_ZLEMA_Fast[1] >= v_ZLEMA_Slow[1] and
                 v_ZLEMA_Fast    <  v_ZLEMA_Slow;

v_Golden_Cross = v_ZLEMA_Fast[1] <= v_ZLEMA_Slow[1] and
                 v_ZLEMA_Fast    >  v_ZLEMA_Slow;


    { SECTION 5.5 - RE-ENTRY STATE MANAGEMENT (v1.6)  -- md 3.38 }

if v_Prev_MP = -1 and MarketPosition = 0 then begin
    v_ReEntry_Armed = True;
    v_ReEntry_Price = v_Last_EntryPrice;
end;

if v_ReEntry_Armed = True then begin
    if v_Golden_Cross = True or v_ZLEMA_Fast >= v_ZLEMA_Slow then begin
        v_ReEntry_Armed = False;
        v_ReEntry_Price = 0;
    end;
end;

if v_Death_Cross = True then begin
    v_ReEntry_Armed = False;
    v_ReEntry_Price = 0;
end;


{ ============================================================
  SECTION 6 - ATR + STOP DISTANCE
  ============================================================ }

v_ATR      = AvgTrueRange( ATR_Len );


    { SECTION 7 - FROZEN SL SETUP + SetStopContract + SetStopLoss + SL_Pct (Rule  -- md 3.39 }

if MarketPosition = -1 then begin
    v_Last_EntryPrice = EntryPrice;
    if v_SL_Locked = False then begin
        { v1.8.0: re-entry inherits original frozen ATR }
        if v_IsReEntry = True and v_Original_Frozen_ATR > 0 then
            v_Frozen_ATR = v_Original_Frozen_ATR
        else begin
            v_Frozen_ATR = v_ATR;
            v_Original_Frozen_ATR = v_ATR;
        end;
        v_Frozen_SL_Dist = v_Frozen_ATR * StopATRMult;
        v_SL_Level       = EntryPrice + v_Frozen_SL_Dist;
        if SL_Pct > 0 then begin
            v_SL_Pct_Ceil = EntryPrice + (EntryPrice * SL_Pct / 100);
            v_SL_Level = MinList(v_SL_Level, v_SL_Pct_Ceil);
        end;
        v_SL_Locked      = True;
        v_EntryBar        = CurrentBar;
        v_QuickStop_Fired = False;
        v_BE_Active       = False;
        { v1.12.0: trail state resets per entry, re-entry included }
        v_MaxProfit       = 0;
        v_Trail_Active    = False;
        v_Trail_Binding   = False;
        { v1.13.0: structure leg state, same per-entry reset }
        v_Struct_Active   = False;
        v_Struct_Lock     = 0;
        v_Struct_Binding  = False;
    end;
end
else begin
    v_SL_Locked          = False;
    v_Frozen_ATR         = 0;
    v_Frozen_SL_Dist     = 0;
    v_SL_Level           = 0;
    v_EntryBar           = 0;
    v_QuickStop_Fired    = False;
    v_BE_Active          = False;
    v_MaxProfit          = 0;
    v_Trail_Active       = False;
    v_Trail_Binding      = False;
    v_Struct_Active      = False;
    v_Struct_Lock        = 0;
    v_Struct_Binding     = False;
end;

    { Rule #12 SetStopContract + SetStopLoss + SL_Pct (short variant: MP >= 0 gu  -- md 3.40 }
v_Guard_Distance = v_ATR * StopATRMult;
if SL_Pct > 0 then
    v_Guard_Distance = MinList(v_Guard_Distance, Close * SL_Pct / 100);

if MarketPosition >= 0 then begin
    SetStopContract;
    SetStopLoss( v_Guard_Distance * BigPointValue );
end;


    { SECTION 8 - M6 MULTI-LAYER INDICATOR PRECOMPUTE  -- md 3.41 }

v_ML_AvgVol    = Average( Volume, ML_VolAvgLen );
v_ML_ATR_Short = AvgTrueRange( ML_ATR_ShortLen );
v_ML_ATR_Long  = AvgTrueRange( ML_ATR_LongLen );
v_ML_RSI       = RSI( Close, 14 );
v_ML_MA_Short  = Average( Close, ML_MA_ShortLen );


    { SECTION 8.5 - SLOPE AS dy/dx (v1.19.1)  -- md 3.42 }
if CurrentBar > 1 then begin
    v_Elapsed_Min = TimeToMinutes( Time ) - TimeToMinutes( Time[1] );
    if v_Elapsed_Min <= 0 then
        v_Elapsed_Min = v_Elapsed_Min + 1440;
end
else
    v_Elapsed_Min = 0;

    { ---- is this a legitimate measurement at all ----  -- md 3.43 }
if BarInterval > 0 then
    v_Slope_Adjacent = ( v_Elapsed_Min = BarInterval )
else
    v_Slope_Adjacent = False;

    { ---- Slope_Form = 2 : the rate form --------------------------  -- md 3.44 }
if Close <> 0 and v_Elapsed_Min > 0 then
    v_Slope_Rate = ( ( Close[1] - Close ) / Close * 100 ) / v_Elapsed_Min
else
    v_Slope_Rate = 0;

    { ---- v1.24.0: the effective rate threshold ----  -- md 3.45 }
v_MinSlope_Eff = MinSlope_Rate;
if v_UpGap_Relax = True then
    v_MinSlope_Eff = MinSlope_Rate * UpGap_Relax_Mult;

    { ---- which form gates the entry ----  -- md 3.46 }
if Slope_Form = 2 then
    v_Slope_Pass = v_Slope_Rate > v_MinSlope_Eff
else
    v_Slope_Pass = v_Slope      > MinSlope;

    { ---- the dx guard, as an entry block ----  -- md 3.47 }
v_FirstBar_Block = False;
if v_Slope_Require_Adjacent = True and v_Slope_Adjacent = False then
    v_FirstBar_Block = True;



    { SECTION 8.6 - K-BAR PATTERN SELECTOR (v1.26.0) }
    { 28 patterns. First match writes its code and stops. Gate is code = 0.  }
    { Thresholds are pinned and must never be swept: long body >= 50% of     }
    { range, doji < 10% of range with range >= 3 points and each shadow >= 1 }
    { point, "equal" means EXACTLY equal, "star" means half the reference    }
    { body. Derivations in docs/research/S16S_kbar_pattern_spec_20260816.md. }
    {                                                                        }
    { Every pattern is gated on v_Bars_In_Sess so no comparison reaches      }
    { across a session break -- the bar before a session open is a different }
    { market, and the gap programme already covers that boundary.            }

v_KB_Code = 0;

if v_KB_On = True and v_Bars_In_Sess >= 2 then begin

    v_KB_R0 = H    - L;
    v_KB_R1 = H[1] - L[1];
    v_KB_B0 = AbsValue( C    - O    );
    v_KB_B1 = AbsValue( C[1] - O[1] );
    v_KB_T0 = MaxList( O,    C    );
    v_KB_T1 = MaxList( O[1], C[1] );
    v_KB_M0 = MinList( O,    C    );
    v_KB_M1 = MinList( O[1], C[1] );
    v_KB_S0 = H    - v_KB_T0;
    v_KB_S1 = H[1] - v_KB_T1;
    v_KB_L0 = ( v_KB_R0 > 0 ) and ( v_KB_R0 >= KB_Pat_MinRange )
              and ( v_KB_B0 / MaxList( v_KB_R0, 1 ) >= KB_Pat_LongBody );
    v_KB_L1 = ( v_KB_R1 > 0 ) and ( v_KB_R1 >= KB_Pat_MinRange )
              and ( v_KB_B1 / MaxList( v_KB_R1, 1 ) >= KB_Pat_LongBody );
    v_KB_D0 = ( v_KB_R0 >= KB_Pat_DojiMinRange )
              and ( v_KB_B0 / MaxList( v_KB_R0, 1 ) < KB_Pat_DojiRatio )
              and ( v_KB_S0 >= KB_Pat_DojiMinShadow )
              and ( v_KB_M0 - L >= KB_Pat_DojiMinShadow );

    if v_Bars_In_Sess >= 3 then begin
        v_KB_R2 = H[2] - L[2];
        v_KB_B2 = AbsValue( C[2] - O[2] );
        v_KB_T2 = MaxList( O[2], C[2] );
        v_KB_M2 = MinList( O[2], C[2] );
        v_KB_S2 = H[2] - v_KB_T2;
        v_KB_N2 = v_KB_M2 - L[2];
        v_KB_L2 = ( v_KB_R2 > 0 ) and ( v_KB_R2 >= KB_Pat_MinRange )
                  and ( v_KB_B2 / MaxList( v_KB_R2, 1 ) >= KB_Pat_LongBody );
    end;

    if v_Bars_In_Sess >= 5 then begin
        v_KB_R3 = H[3] - L[3];
        v_KB_R4 = H[4] - L[4];
        v_KB_B3 = AbsValue( C[3] - O[3] );
        v_KB_B4 = AbsValue( C[4] - O[4] );
        v_KB_L3 = ( v_KB_R3 > 0 ) and ( v_KB_R3 >= KB_Pat_MinRange )
                  and ( v_KB_B3 / MaxList( v_KB_R3, 1 ) >= KB_Pat_LongBody );
        v_KB_L4 = ( v_KB_R4 > 0 ) and ( v_KB_R4 >= KB_Pat_MinRange )
                  and ( v_KB_B4 / MaxList( v_KB_R4, 1 ) >= KB_Pat_LongBody );
    end;


    { ---------- CODES 1-13   PRO-SHORT ---------- }

if v_KB_Sup = True then begin

    { 1  A01 bearish engulfing }
    if v_KB_Code = 0 and ( C[1] > O[1] ) and ( C < O )
       and ( O >= C[1] ) and ( C <= O[1] ) and ( v_KB_B0 > v_KB_B1 ) then
        v_KB_Code = 1;

    { 2  A05 bearish harami }
    if v_KB_Code = 0 and ( C[1] > O[1] ) and ( v_KB_L1 = True ) and ( C < O )
       and ( v_KB_T0 <= C[1] ) and ( v_KB_M0 >= O[1] ) then
        v_KB_Code = 2;

    { 3  A07 bearish harami cross }
    if v_KB_Code = 0 and ( C[1] > O[1] ) and ( v_KB_L1 = True ) and ( v_KB_D0 = True )
       and ( v_KB_T0 <= C[1] ) and ( v_KB_M0 >= O[1] ) then
        v_KB_Code = 3;

    { 4  A18 tweezer top -- highs EXACTLY equal }
    if v_KB_Code = 0 and ( H = H[1] ) and ( C[1] > O[1] ) and ( C < O ) then
        v_KB_Code = 4;

    { 5  A20 matching high -- closes EXACTLY equal }
    if v_KB_Code = 0 and ( C[1] > O[1] ) and ( v_KB_L1 = True ) and ( C > O )
       and ( O > O[1] ) and ( C = C[1] ) then
        v_KB_Code = 5;

    if v_Bars_In_Sess >= 3 then begin

        { 6  B25 three black crows -- each opens INSIDE the prior body }
        if v_KB_Code = 0 and ( C[2] < O[2] ) and ( C[1] < O[1] ) and ( C < O )
           and ( v_KB_L2 = True ) and ( v_KB_L1 = True ) and ( v_KB_L0 = True )
           and ( C[1] < C[2] ) and ( C < C[1] )
           and ( O[1] < O[2] ) and ( O[1] > C[2] )
           and ( O < O[1] )    and ( O > C[1] ) then
            v_KB_Code = 6;

        { 7  B27 three inside down }
        if v_KB_Code = 0 and ( C[2] > O[2] ) and ( v_KB_L2 = True ) and ( C[1] < O[1] )
           and ( v_KB_T1 <= C[2] ) and ( v_KB_M1 >= O[2] )
           and ( C < O ) and ( C < C[1] ) then
            v_KB_Code = 7;

        { 8  B29 three outside down }
        if v_KB_Code = 0 and ( C[2] > O[2] ) and ( C[1] < O[1] )
           and ( O[1] >= C[2] ) and ( C[1] <= O[2] ) and ( v_KB_B1 > v_KB_B2 )
           and ( C < O ) and ( C < C[1] ) then
            v_KB_Code = 8;

        { 9  B34 three identical crows -- each opens EXACTLY at the prior close }
        if v_KB_Code = 0 and ( C[2] < O[2] ) and ( C[1] < O[1] ) and ( C < O )
           and ( v_KB_L2 = True ) and ( v_KB_L1 = True ) and ( v_KB_L0 = True )
           and ( O[1] = C[2] ) and ( O = C[1] )
           and ( C[1] < C[2] ) and ( C < C[1] ) then
            v_KB_Code = 9;

        { 10  B35 advance block -- bodies shrink, upper shadows grow }
        if v_KB_Code = 0 and ( C[2] > O[2] ) and ( C[1] > O[1] ) and ( C > O )
           and ( C[1] > C[2] ) and ( C > C[1] )
           and ( v_KB_B1 < v_KB_B2 ) and ( v_KB_B0 < v_KB_B1 )
           and ( v_KB_S1 > 0 ) and ( v_KB_S0 > 0 ) and ( v_KB_S0 > v_KB_S2 ) then
            v_KB_Code = 10;

        { 11  B36 deliberation -- third body under HALF the second }
        if v_KB_Code = 0 and ( C[2] > O[2] ) and ( C[1] > O[1] ) and ( C > O )
           and ( v_KB_L2 = True ) and ( v_KB_L1 = True )
           and ( C[1] > C[2] ) and ( C > C[1] )
           and ( v_KB_B0 < v_KB_B1 / 2 ) and ( O >= C[1] ) then
            v_KB_Code = 11;
    end;

    { 12  C41 falling three methods -- five bars }
    if v_KB_Code = 0 and v_Bars_In_Sess >= 5
       and ( C[4] < O[4] ) and ( v_KB_L4 = True )
       and ( C[3] > O[3] ) and ( C[2] > O[2] ) and ( C[1] > O[1] )
       and ( v_KB_B3 < v_KB_B4 ) and ( v_KB_B2 < v_KB_B4 ) and ( v_KB_B1 < v_KB_B4 )
       and ( H[3] <= H[4] ) and ( L[3] >= L[4] )
       and ( H[2] <= H[4] ) and ( L[2] >= L[4] )
       and ( H[1] <= H[4] ) and ( L[1] >= L[4] )
       and ( C < O ) and ( v_KB_L0 = True )
       and ( O > C[1] ) and ( C < C[4] ) then
        v_KB_Code = 12;

    { 13  C45 bearish three-line strike -- four bars }
    if v_KB_Code = 0 and v_Bars_In_Sess >= 4
       and ( C[3] > O[3] ) and ( C[2] > O[2] ) and ( C[1] > O[1] )
       and ( O[2] > O[3] ) and ( O[1] > O[2] )
       and ( C[2] > C[3] ) and ( C[1] > C[2] )
       and ( C < O ) and ( O > C[1] ) and ( C < O[3] ) then
        v_KB_Code = 13;
end;


    { ---------- CODES 14-26   ANTI-SHORT ---------- }
    { All but 18 and 22 end on a white bar and cannot fire while the slope   }
    { gate holds. They are coded so MC12 can prove that, not assume it.      }

if v_KB_Opp = True then begin

    { 14  A02 bullish engulfing }
    if v_KB_Code = 0 and ( C[1] < O[1] ) and ( C > O )
       and ( O <= C[1] ) and ( C >= O[1] ) and ( v_KB_B0 > v_KB_B1 ) then
        v_KB_Code = 14;

    { 15  A06 bullish harami }
    if v_KB_Code = 0 and ( C[1] < O[1] ) and ( v_KB_L1 = True ) and ( C > O )
       and ( v_KB_T0 <= O[1] ) and ( v_KB_M0 >= C[1] ) then
        v_KB_Code = 15;

    { 16  A08 bullish harami cross }
    if v_KB_Code = 0 and ( C[1] < O[1] ) and ( v_KB_L1 = True ) and ( v_KB_D0 = True )
       and ( v_KB_T0 <= O[1] ) and ( v_KB_M0 >= C[1] ) then
        v_KB_Code = 16;

    { 17  A19 tweezer bottom -- lows EXACTLY equal }
    if v_KB_Code = 0 and ( L = L[1] ) and ( C[1] < O[1] ) and ( C > O ) then
        v_KB_Code = 17;

    { 18  A21 matching low -- closes EXACTLY equal. BLACK last bar, so this  }
    {     is the one anti-short pattern the slope gate does not exclude.     }
    if v_KB_Code = 0 and ( C[1] < O[1] ) and ( v_KB_L1 = True ) and ( C < O )
       and ( O < O[1] ) and ( C = C[1] ) then
        v_KB_Code = 18;

    if v_Bars_In_Sess >= 3 then begin

        { 19  B26 three white soldiers -- each opens INSIDE the prior body }
        if v_KB_Code = 0 and ( C[2] > O[2] ) and ( C[1] > O[1] ) and ( C > O )
           and ( v_KB_L2 = True ) and ( v_KB_L1 = True ) and ( v_KB_L0 = True )
           and ( C[1] > C[2] ) and ( C > C[1] )
           and ( O[1] > O[2] ) and ( O[1] < C[2] )
           and ( O > O[1] )    and ( O < C[1] ) then
            v_KB_Code = 19;

        { 20  B28 three inside up }
        if v_KB_Code = 0 and ( C[2] < O[2] ) and ( v_KB_L2 = True ) and ( C[1] > O[1] )
           and ( v_KB_T1 <= O[2] ) and ( v_KB_M1 >= C[2] )
           and ( C > O ) and ( C > C[1] ) then
            v_KB_Code = 20;

        { 21  B30 three outside up }
        if v_KB_Code = 0 and ( C[2] < O[2] ) and ( C[1] > O[1] )
           and ( O[1] <= C[2] ) and ( C[1] >= O[2] ) and ( v_KB_B1 > v_KB_B2 )
           and ( C > O ) and ( C > C[1] ) then
            v_KB_Code = 21;

        { 22  B37 three stars in the south. BLACK last bar -- the second     }
        {     anti-short pattern the slope gate does not exclude.            }
        if v_KB_Code = 0 and ( C[2] < O[2] ) and ( C[1] < O[1] ) and ( C < O )
           and ( v_KB_L2 = True ) and ( v_KB_N2 > 0 )
           and ( O[1] < O[2] ) and ( O[1] > C[2] ) and ( L[1] > L[2] )
           and ( v_KB_B1 < v_KB_B2 )
           and ( H <= H[1] ) and ( L >= L[1] ) and ( v_KB_B0 < v_KB_B1 ) then
            v_KB_Code = 22;

        { 23  B38 unique three river bottom }
        if v_KB_Code = 0 and ( C[2] < O[2] ) and ( v_KB_L2 = True ) and ( C[1] < O[1] )
           and ( O[1] <= O[2] ) and ( O[1] >= C[2] ) and ( L[1] < L[2] )
           and ( C[1] > C[2] )
           and ( C > O ) and ( v_KB_B0 < v_KB_B1 ) and ( C < C[1] ) then
            v_KB_Code = 23;
    end;

    { 24  C42 rising three methods -- five bars }
    if v_KB_Code = 0 and v_Bars_In_Sess >= 5
       and ( C[4] > O[4] ) and ( v_KB_L4 = True )
       and ( C[3] < O[3] ) and ( C[2] < O[2] ) and ( C[1] < O[1] )
       and ( v_KB_B3 < v_KB_B4 ) and ( v_KB_B2 < v_KB_B4 ) and ( v_KB_B1 < v_KB_B4 )
       and ( H[3] <= H[4] ) and ( L[3] >= L[4] )
       and ( H[2] <= H[4] ) and ( L[2] >= L[4] )
       and ( H[1] <= H[4] ) and ( L[1] >= L[4] )
       and ( C > O ) and ( v_KB_L0 = True )
       and ( O < C[1] ) and ( C > C[4] ) then
        v_KB_Code = 24;

    { 25  C43 ladder bottom -- five bars }
    if v_KB_Code = 0 and v_Bars_In_Sess >= 5
       and ( C[4] < O[4] ) and ( C[3] < O[3] ) and ( C[2] < O[2] )
       and ( v_KB_L4 = True ) and ( v_KB_L3 = True ) and ( v_KB_L2 = True )
       and ( O[3] < O[4] ) and ( O[2] < O[3] )
       and ( C[3] < C[4] ) and ( C[2] < C[3] )
       and ( C[1] < O[1] ) and ( v_KB_S1 >= v_KB_B1 )
       and ( C > O ) and ( O > O[1] ) then
        v_KB_Code = 25;

    { 26  C46 bullish three-line strike -- four bars }
    if v_KB_Code = 0 and v_Bars_In_Sess >= 4
       and ( C[3] < O[3] ) and ( C[2] < O[2] ) and ( C[1] < O[1] )
       and ( O[2] < O[3] ) and ( O[1] < O[2] )
       and ( C[2] < C[3] ) and ( C[1] < C[2] )
       and ( C > O ) and ( O < C[1] ) and ( C > O[3] ) then
        v_KB_Code = 26;
end;


    { ---------- CODES 27-28   NEUTRAL ---------- }
    { Colour-blind, so both can fire on a black bar. A10 outside bar is the  }
    { second most frequent pattern on actual entries -- 28 of the 180.       }

if v_KB_Neu = True then begin

    { 27  A09 inside bar }
    if v_KB_Code = 0 and ( H <= H[1] ) and ( L >= L[1] ) then
        v_KB_Code = 27;

    { 28  A10 outside bar }
    if v_KB_Code = 0 and ( H >= H[1] ) and ( L <= L[1] ) then
        v_KB_Code = 28;
end;

end;

v_KB_Block = ( v_KB_Code <> 0 );


    { SECTION 8.7 - FILTER ORDER (v1.26.0) }
    { The slope gate and the K-bar gate are both pure functions of the CURRENT }
    { bar and neither reads the other, so ANDing them is commutative: "slope   }
    { first" and "pattern first" select the identical set of bars. Order can   }
    { only matter if one of them is allowed to persist, so each gets an age.   }
    {                                                                          }
    {   age 0     the condition holds on THIS bar                              }
    {   age n     it last held n bars ago                                      }
    {   age 999   it has not held since the session opened                     }
    {                                                                          }
    { KB_Filter_Win = 0 forces both ages to 0, which is the plain same-bar AND,}
    { so every mode collapses to the anchor and the anchor cannot drift.       }

if v_Slope_Pass = True then
    v_Slope_Age = 0
else if v_Bars_In_Sess <= 1 then
    v_Slope_Age = 999
else
    v_Slope_Age = v_Slope_Age + 1;

if v_KB_Block = False then
    v_Clean_Age = 0
else if v_Bars_In_Sess <= 1 then
    v_Clean_Age = 999
else
    v_Clean_Age = v_Clean_Age + 1;

if KB_Filter_Mode = 1 then
        { slope is the first filter: it may be up to Win bars old, and the    }
        { pattern check must be clean on the entry bar itself                 }
    v_Filt_Pass = ( v_Slope_Age <= KB_Filter_Win ) and ( v_Clean_Age = 0 )
else if KB_Filter_Mode = 2 then
        { pattern is the first filter: a clean bar arms for Win bars, and the }
        { slope must hold on the entry bar itself                            }
    v_Filt_Pass = ( v_Clean_Age <= KB_Filter_Win ) and ( v_Slope_Age = 0 )
else if KB_Filter_Mode = 3 then
        { independent: whichever fires first arms, the other confirms inside  }
        { the window                                                          }
    v_Filt_Pass = ( ( v_Slope_Age <= KB_Filter_Win ) and ( v_Clean_Age = 0 ) )
               or ( ( v_Clean_Age <= KB_Filter_Win ) and ( v_Slope_Age = 0 ) )
else
        { mode 0 -- both on this bar, which is v1.25.0 behaviour exactly      }
    v_Filt_Pass = ( v_Slope_Age = 0 ) and ( v_Clean_Age = 0 );


    { SECTION 9 - ENTRY (Death Cross + M1 Slope + Rule #11 gates)  -- md 3.48 }
if MarketPosition = 0 and
   v_Death_Cross         = True and
   v_Filt_Pass           = True and
   v_FirstBar_Block      = False and
   v_Tail_Entry_OK       = True and
   v_Settlement_Day      = False and
   v_Holiday_Block       = False and
   v_Registry_Expired    = False and
   Manual_Kill_Switch    = False and
   v_Gap_Block           = False and
   v_Sess_LastBar_Block  = False then begin
    v_IsReEntry = False;
    sell short ( "SE_MA_DeathCross" ) next bar at market;
end;

    { 2026-08-20 DEFECT FIX. The two entry paths were using DIFFERENT K-bar     }
    { rules. The death cross gate reads v_Filt_Pass, which at Win = 3 accepts a  }
    { clean bar up to three bars back. Re-entry read v_KB_Block directly, which  }
    { demands the entry bar ITSELF be clean -- STRICTER than the path the sweep  }
    { actually measured. That asymmetry was not a decision; the flag was added   }
    { on 2026-08-19 after the death cross gate had already moved to v_Filt_Pass. }
if ReEntry_Filt_Mode <> 0 then
    v_ReEntry_Filt_OK = v_Filt_Pass
else
    v_ReEntry_Filt_OK = ( v_KB_Block = False );

    { v1.6 Re-Entry: washout recovery via stop order at original entry price.  -- md 3.49 }
if MarketPosition = 0 and
   v_ReEntry_On            = True and
   v_ReEntry_Armed       = True and
   ( v_ReEntry_Structure_Gate = False or v_ZLEMA_Fast < v_ZLEMA_Slow ) and
   ( v_ReEntry_Slope_Gate     = False or v_Slope_Pass = True ) and
   v_ReEntry_Filt_OK     = True and
   v_FirstBar_Block      = False and
   ( v_ReEntry_Close_Gate     = False or Close        >= v_ReEntry_Price ) and
   v_Tail_Entry_OK       = True and
   v_Settlement_Day      = False and
   v_Holiday_Block       = False and
   v_Registry_Expired    = False and
   Manual_Kill_Switch    = False and
   v_Gap_Block           = False and
   v_Sess_LastBar_Block  = False then begin
    v_IsReEntry = True;
    sell short ( "SE_MA_ReEntry" ) next bar at v_ReEntry_Price stop;
end;


    { SECTION 10 - EXIT PRIORITY CHAIN (P0 to P7)  -- md 3.50 }

if MarketPosition = -1 then begin

    ExitFired = 0;
    v_BarsSince = CurrentBar - v_EntryBar;
    v_Loss = Close - EntryPrice;       { short: positive = loss }
    v_Profit = EntryPrice - Close;     { short: positive = profit }

    { ---- P0: Priority 0 compliance ---- }
    if Manual_Kill_Switch = True then begin
        buy to cover ( "SX_MA_Kill" ) next bar at market;
        ExitFired = 1;
    end;

    if ExitFired = 0 and v_Registry_Expired = True then begin
        buy to cover ( "SX_MA_Registry" ) next bar at market;
        ExitFired = 1;
    end;

    if ExitFired = 0 and v_Holiday_Block = True and
       Time >= Holiday_Flat_Time then begin
        buy to cover ( "SX_MA_Holiday" ) next bar at market;
        ExitFired = 1;
    end;

    if ExitFired = 0 and v_Settlement_Day = True and
       Time >= Settlement_Flat_Time then begin
        buy to cover ( "SX_MA_Settlement" ) next bar at market;
        ExitFired = 1;
    end;

    { ---- P0.5: Tail Force Exit (v1.3) - flat before session close ----  -- md 3.51 }
    if ExitFired = 0 and
       Time >= Tail_ForceExit_Time and Time <= 500 then begin
        buy to cover ( "SX_MA_TailFlat" ) next bar at market;
        ExitFired = 1;
    end;

    { ---- P1: M5 Quick Stop ---- }
    if ExitFired = 0 and v_QuickStop_On = True and
       v_QuickStop_Fired = False then begin

        { P1a Loss-based (v1.7.1: pct of EntryPrice, not fixed pts) }
        if v_QS_Loss_On = True and
           v_Loss > EntryPrice * QS_MaxLoss_Pct / 100 then begin
            buy to cover ( "SX_MA_QuickStop_Loss" ) next bar at market;
            v_QuickStop_Fired = True;
            ExitFired = 1;
        end;

        { P1b Time-based (N bars with no profit) }
        if ExitFired = 0 and v_QS_Time_On = True and
           v_BarsSince >= QuickStop_MaxBars and
           Close >= EntryPrice then begin
            buy to cover ( "SX_MA_QuickStop_Time" ) next bar at market;
            v_QuickStop_Fired = True;
            ExitFired = 1;
        end;
    end;

    { ---- P2: M6 Rule #17 Multi-Layer 1M Monitor ---- }
    if ExitFired = 0 and v_ML_On = True then begin

        { v1.8.0: use frozen SL dist, not unfrozen v_StopDist }
        v_ML_Activation_Level = v_Frozen_SL_Dist * ML_ActivationPct / 100;

        if v_Loss > v_ML_Activation_Level then begin

            { Reset all category scores }
            v_ML_Cat_1F = 0;
            v_ML_Cat_2F = 0;
            v_ML_Cat_3F = 0;
            v_ML_Cat_4F = 0;
            v_ML_Cat_5F = 0;

            { 1F K-bar layer: bullish engulfing (adverse for short) }
            if Close > High[1] and Close > Open then
                v_ML_Cat_1F = 1;

            { 2F Vol-price layer: volume spike }
            if Volume > v_ML_AvgVol * ML_VolSpikeMult then
                v_ML_Cat_2F = 1;

            { 3F Momentum layer: RSI reversal from oversold }
            v_ML_RSI_Prior = Lowest( RSI( Close, 14 ), 5 );
            if v_ML_RSI_Prior < 30 and v_ML_RSI > 40 then
                v_ML_Cat_3F = 1;

            { 4F Structure layer: higher low + close above short MA }
            if Low > Low[1] and Close > v_ML_MA_Short then
                v_ML_Cat_4F = 1;

            { 5F Volatility layer: ATR expansion }
            if v_ML_ATR_Long > 0 and
               v_ML_ATR_Short / v_ML_ATR_Long > ML_ATR_Ratio then
                v_ML_Cat_5F = 1;

            { Compute score }
            v_ML_Score    = v_ML_Cat_1F + v_ML_Cat_2F + v_ML_Cat_3F +
                            v_ML_Cat_4F + v_ML_Cat_5F;
            v_ML_Score_Pct = v_ML_Score / 5 * 100;
            v_ML_CatCount = v_ML_Score;   { 1 factor per category = same }

            v_ML_Trigger = v_ML_Score_Pct >= ML_ScoreTrigger and
                           v_ML_CatCount >= ML_MinCategories;

            if v_ML_Trigger = True then begin
                buy to cover ( "SX_MA_ML_Exit" ) next bar at market;
                ExitFired = 1;
            end;
        end;
    end;

    { ---- P3: M7 True Breakeven + P3b Profit Trail (v1.12.0) ----  -- md 3.52 }
    if v_Profit > v_MaxProfit then
        v_MaxProfit = v_Profit;

    { Latch activations }
    if v_BE_On = True and
       v_Profit >= EntryPrice * BE_Trigger_Pct / 100 then
        v_BE_Active = True;

    if v_Trail_On = True and
       v_MaxProfit >= EntryPrice * Trail_Trigger_Pct / 100 then
        v_Trail_Active = True;

    if v_Struct_On = True and
       v_MaxProfit >= EntryPrice * Struct_Trigger_Pct / 100 then
        v_Struct_Active = True;

    { Resolve the binding lock }
    v_Protect_Lock   = 0;
    v_Trail_Binding  = False;
    v_Struct_Binding = False;

    if v_BE_On = True and v_BE_Active = True then
        v_Protect_Lock = BE_Cost_Pts;

    if v_Trail_On = True and v_Trail_Active = True then begin
    { v1.14.0 B1 tiered giveback. One tier, two parameters -- with  -- md 3.53 }
        v_Trail_Giveback = Trail_Giveback_Pct;
        if v_Tier2_On = True and
           v_MaxProfit >= EntryPrice * Tier2_Trigger_Pct / 100 then
            v_Trail_Giveback = Tier2_Giveback_Pct;

        v_Trail_Lock = v_MaxProfit * ( 100 - v_Trail_Giveback ) / 100;
        if v_Trail_Lock > v_Protect_Lock then begin
            v_Protect_Lock  = v_Trail_Lock;
            v_Trail_Binding = True;
        end;
    end;

    { v1.13.0 A2 structure leg.  -- md 3.54 }
    if v_Struct_On = True and v_Struct_Active = True then begin

        v_Struct_Window = MinList( Struct_Lookback, v_BarsSince + 1 );
        if v_Struct_Window < 1 then
            v_Struct_Window = 1;

        if v_Struct_UseClose = True then
            v_Struct_Ref = Highest( Close, v_Struct_Window )
        else
            v_Struct_Ref = Highest( High, v_Struct_Window );

        v_Struct_Raw = EntryPrice - ( v_Struct_Ref + Struct_Buffer_Pts );

        { latch: monotonic non-decreasing, never retreats }
        if v_Struct_Raw > v_Struct_Lock then
            v_Struct_Lock = v_Struct_Raw;

        if v_Struct_Lock > v_Protect_Lock then begin
            v_Protect_Lock   = v_Struct_Lock;
            v_Trail_Binding  = False;
            v_Struct_Binding = True;
        end;
    end;

    if ExitFired = 0 and v_Protect_Lock > 0 then begin
        v_BE_Stop_Level = EntryPrice - v_Protect_Lock;
        if v_Struct_Binding = True then
            buy to cover ( "SX_MA_Struct" ) next bar at v_BE_Stop_Level stop
        else if v_Trail_Binding = True then
            buy to cover ( "SX_MA_Trail" ) next bar at v_BE_Stop_Level stop
        else
            buy to cover ( "SX_MA_BE" ) next bar at v_BE_Stop_Level stop;
    end;

    { ---- P4: Golden Cross main exit ---- }
    if ExitFired = 0 and v_Golden_Cross = True then begin
        buy to cover ( "SX_MA_GoldenCross" ) next bar at market;
        ExitFired = 1;
    end;

    { ---- P5: M8 Hold Cap -- v1.25.0 TWO FORMS ----  -- md 3.55 }
    if ExitFired = 0 and v_M8_On = True then begin

        if M8_Form = 2 then begin
            v_M8_Target = EntryPrice - EntryPrice * MaxHold_Pct / 100;
            buy to cover ( "SX_MA_TimeStop_Pct" ) next bar at v_M8_Target limit;
        end;

        if v_BarsSince >= MaxHoldingBars then begin
            buy to cover ( "SX_MA_TimeStop" ) next bar at market;
            ExitFired = 1;
        end;
    end;

    { ---- P6: ATR-based Frozen SL (last resort) ---- }
    if ExitFired = 0 and v_SL_Locked = True then begin
        buy to cover ( "SX_MA_SL" ) next bar at v_SL_Level Stop;
    end;

    { P7 handled by SetStopContract + SetStopLoss + SL_Pct in Section 7 }

end;


{ ============================================================
  SECTION 11 - PREV_MP TRACKING (last line, per PL Rule 8)
  ============================================================ }
v_Prev_MP = MarketPosition;
