# S1 NightMomentum — 中文逐行註解

> 對應程式碼：`S1_NightMomentum.pla`（MC12 直接使用的全英文版）
> 最後更新：2026-06-07
> 參數來源：Walk-Forward 優化結果

---

## 策略概述
- **類別**：A 類（時段型）
- **方向**：雙向（做多 + 做空）
- **週期**：15 分鐘
- **核心邏輯**：夜盤開盤前 N 根 K 棒形成「開盤區間」，突破高點做多、跌破低點做空

---

## 參數說明

| 參數 | WF 最佳值 | 原始值 | 說明 |
|------|----------|--------|------|
| `LookbackBars` | **6** | 4 | 夜盤開盤後幾根 K 棒形成區間（6×15M = 90分鐘） |
| `EntryOffset` | **6** | 10 | 突破區間高/低後加幾點才進場（過濾假突破） |
| `MinRange` | 30 | 30 | 區間寬度最低門檻（太窄 = 無方向性） |
| `MaxRange` | 200 | 200 | 區間寬度最高門檻（太寬 = 已走完行情） |
| `StopLossPts` | **80** | 60 | 固定停損點數 |
| `TakeProfitPts` | **160** | 120 | 固定停利點數 |
| `TrailActivate` | 50 | 50 | 獲利超過幾點啟動追蹤停損 |
| `TrailOffset` | 30 | 30 | 追蹤停損回撤幾點平倉 |
| `NightOpen` | 1500 | 1500 | 夜盤開盤時間（15:00） |
| `ExitTime` | 0430 | 0430 | 強制平倉時間（04:30） |

---

## 逐段邏輯註解

### 1. 偵測夜盤時段
```
v_IsNightSession = (Time >= NightOpen) or (Time < ExitTime);
```
- 15:00 以後 **或** 04:30 以前 → 判定為夜盤

### 2. 夜盤開盤重置
```
if Time >= NightOpen and Time[1] < NightOpen then begin
    v_NightHigh = 0;
    v_NightLow = 999999;
    v_NightBarCount = 0;
    v_RangeReady = false;
end;
```
- 當時間跨過 15:00 → 重置所有變數
- `Time[1] < NightOpen`：前一根 K 棒還不是夜盤 → 確定是剛開盤

### 3. 建構開盤區間
```
if v_IsNightSession and v_RangeReady = false then begin
    v_NightBarCount = v_NightBarCount + 1;
    if High > v_NightHigh then v_NightHigh = High;
    if Low < v_NightLow then v_NightLow = Low;
    if v_NightBarCount >= LookbackBars then begin
        v_RangeWidth = v_NightHigh - v_NightLow;
        if v_RangeWidth >= MinRange and v_RangeWidth <= MaxRange then
            v_RangeReady = true;
    end;
end;
```
- 逐根累計最高/最低價
- 滿 6 根後計算區間寬度
- 區間寬度在 30~200 點之間才啟動 → 過濾過窄（無方向）或過寬（行情已走完）的情況

### 4. 進場：區間突破
```
if v_RangeReady and v_IsNightSession and Time < ExitTime then begin
    if v_Prev_MP <= 0 then
        buy ("LE_NM_Long") next bar at v_NightHigh + EntryOffset stop;
    if v_Prev_MP >= 0 then
        sell short ("SE_NM_Short") next bar at v_NightLow - EntryOffset stop;
end;
```
- **做多**：價格觸及「區間高點 + 6 點」→ Stop 單進場
- **做空**：價格觸及「區間低點 - 6 點」→ Stop 單進場
- `v_Prev_MP` 避免同方向重複進場

### 5. 出場：停損/停利（MC12 內建函數）
```
SetStopLoss(StopLossPts * 200);      // 80 × 200 = 16,000 NTD
SetProfitTarget(TakeProfitPts * 200); // 160 × 200 = 32,000 NTD
```
- MC12 全域停損/停利函數，單位是金額（點數 × 合約乘數 200）

### 6. 出場：追蹤停損
```
if MarketPosition = 1 and MaxContractProfit / 200 >= TrailActivate then
    sell ("LX_NM_Trail") next bar at EntryPrice + TrailActivate - TrailOffset stop;
```
- 做多：獲利達 50 點後，在「進場價 + 50 - 30 = 進場價 + 20」設停損
- 做空：同理，鎖住至少 20 點利潤

### 7. 出場：時間平倉
```
if Time >= ExitTime and Time < NightOpen then begin
    if MarketPosition = 1 then
        sell ("LX_NM_Time") next bar at market;
    if MarketPosition = -1 then
        buy to cover ("SX_NM_Time") next bar at market;
end;
```
- 04:30 後強制平倉，避免留倉到日盤

---

## 進出場標籤對照表

| 標籤 | 指令 | 方向 | 類型 | 觸發條件 |
|------|------|------|------|---------|
| `LE_NM_Long` | `buy` | 做多 | 進場 | 突破區間高點 |
| `SE_NM_Short` | `sell short` | 做空 | 進場 | 跌破區間低點 |
| `LX_NM_Trail` | `sell` | 做多 | 出場 | 追蹤停損觸發 |
| `SX_NM_Trail` | `buy to cover` | 做空 | 出場 | 追蹤停損觸發 |
| `LX_NM_Time` | `sell` | 做多 | 出場 | 04:30 時間平倉 |
| `SX_NM_Time` | `buy to cover` | 做空 | 出場 | 04:30 時間平倉 |
| *(SetStopLoss)* | — | 多/空 | 出場 | 固定 80 點停損 |
| *(SetProfitTarget)* | — | 多/空 | 出場 | 固定 160 點停利 |

> **規則**：`LE_` = Long Entry、`LX_` = Long eXit、`SE_` = Short Entry、`SX_` = Short eXit
> 每個標籤唯一，多空絕不共用。

---

---

## MC12 優化設定卡（完整版）

### 一、交易操作週期

| 項目 | 主要設定 | 替代測試範圍 | 說明 |
|------|---------|-------------|------|
| **Data1** | TXF1 **15 分鐘** | 10M / 15M / 20M / 30M | 夜盤 ORB 核心週期，建議至少測 15M 和 30M 比較 |
| **Data2** | 不需要 | — | 本策略為單一時間框架 |
| **交易時段** | 夜盤 15:00 ~ 隔日 04:30 | — | NightOpen / ExitTime 參數控制 |

> **週期測試建議**：先用 15M 跑完基線，再用 30M 對照。若 30M 表現更穩定（MDD 更低），可考慮切換。10M 交易頻率更高但滑價衝擊更大。

### 二、回測基本設定

| 項目 | 設定 |
|------|------|
| 商品 | TXF1（台指期近月連續） |
| 合約乘數 | 200 NTD/點 |
| 滑價 | 單邊 500 NTD（來回 1,000 NTD） |
| 口數 | 1 口固定 |
| 回測區間 | 2020/01/01 ~ 今天 |
| 初始資金 | 300,000 ~ 500,000 NTD |
| 手續費 | 依券商實際設定（建議含交易稅 10 萬分之 2） |

### 三、所有參數最佳化範圍

| 參數 | 預設值 | WF 最佳值 | 掃描起點 | 掃描終點 | 步長 | 組合數 | Python 高原區 | 說明 |
|------|--------|----------|---------|---------|------|--------|--------------|------|
| `LookbackBars` | 4 | **6** | 2 | 10 | 1 | 9 | 6-10 (44%) | 開盤區間觀察根數。6×15M=90分鐘 |
| `EntryOffset` | 10 | **6** | 0 | 20 | 2 | 11 | 0-18 (69%) | 突破偏移點數，過濾假突破 |
| `MinRange` | 30 | 30 | 20 | 50 | 10 | 4 | 未掃描 | 區間最窄門檻（太窄無方向） |
| `MaxRange` | 200 | 200 | 150 | 300 | 50 | 4 | 未掃描 | 區間最寬門檻（太寬已走完） |
| `StopLossPts` | 60 | **80** | 30 | 120 | 10 | 10 | 70-120 (50%) | 固定停損點數 |
| `TakeProfitPts` | 120 | **160** | 60 | 250 | 10 | 20 | 110-200 (60%) | 固定停利點數 |
| `TrailActivate` | 50 | 50 | 0 | 100 | 10 | 11 | 日線無效，MC12 才能驗證 | 追蹤停損啟動門檻（0=關閉） |
| `TrailOffset` | 30 | 30 | 10 | 50 | 10 | 5 | 日線無效 | 追蹤停損回撤容忍點數 |
| `NightOpen` | 1500 | 1500 | — | — | — | — | 固定 | 夜盤開盤時間 |
| `ExitTime` | 0430 | 0430 | 0400 | 0500 | 30 | 3 | 未掃描 | 強制平倉時間 |

**全參數暴力掃描組合數**：9 × 11 × 4 × 4 × 10 × 20 × 11 × 5 × 3 = **52,272,000+**（暴力不可行，用基因演算法）

### 四、基因演算法（GA）設定

| 設定項目 | 建議值 | 說明 |
|---------|--------|------|
| **最佳化依據** | 最大的 **Net Profit** | 以淨利最大化為目標函數 |
| **演算法** | ◉ 基因演算法 | 52M+ 組合暴力掃描不可行 |
| **族群規模** | **800 ~ 1000** | 總組合數 > 5000 萬，族群需夠大以覆蓋搜尋空間 |
| **交配機率** | **0.90 ~ 0.95** | 高交配率加速收斂 |
| **突變機率** | **0.05 ~ 0.10** | 防止過早收斂到局部最優。參數多(9個)時偏 0.08~0.10 |
| **收斂型態** | ◉ 世代數量 | 用固定世代數，跑完整輪 |
| **最大代數** | **500 ~ 800** | 族群 800 × 代數 600 ≈ 48 萬次評估，覆蓋約 1% 搜尋空間 |
| **Crossover 個體數** | **20** | 每代交叉配對的個體數 |
| **演算法類型** | ◉ 增加的（Incremental） | 逐步替換，收斂更平穩 |
| **更換方案** | ◉ 最差 | 淘汰最差個體，保留精英 |

> **預估執行時間**：族群 800 × 代數 600 = 480,000 次回測。15M TXF1 約 6 年資料，每次回測 ~0.1 秒 → 約 **13 小時**。可視電腦效能調整代數。

#### GA 設定的調整原則（適用所有策略）

| 總組合數 | 族群規模 | 最大代數 | 突變率 | 預估評估次數 |
|---------|---------|---------|--------|------------|
| < 10,000 | 不用 GA，直接暴力 | — | — | 全掃 |
| 10,000 ~ 100,000 | 200 | 200 | 0.05 | 40,000 |
| 100,000 ~ 1,000,000 | 300 ~ 500 | 300 ~ 400 | 0.05 ~ 0.08 | 120,000 |
| 1,000,000 ~ 10,000,000 | 500 ~ 700 | 400 ~ 600 | 0.08 | 280,000 |
| > 10,000,000 | 800 ~ 1,000 | 500 ~ 800 | 0.08 ~ 0.10 | 480,000+ |

### 五、分階段優化順序（替代方案）

如果不想跑 GA 全參數一次掃，也可以分組暴力掃描：

**第一輪：核心進場參數**（先固定出場為 WF 最佳值）
| 參數 | 範圍 | 步長 | 組合數 |
|------|------|------|--------|
| `LookbackBars` | 2 ~ 10 | 1 | 9 |
| `EntryOffset` | 0 ~ 20 | 2 | 11 |
| `MinRange` | 20 ~ 50 | 10 | 4 |
| `MaxRange` | 150 ~ 300 | 50 | 4 |
| **小計** | | | **1,584** |

**第二輪：停損停利**（鎖定第一輪最佳進場參數）
| 參數 | 範圍 | 步長 | 組合數 |
|------|------|------|--------|
| `StopLossPts` | 30 ~ 120 | 10 | 10 |
| `TakeProfitPts` | 60 ~ 250 | 10 | 20 |
| **小計** | | | **200** |

**第三輪：追蹤停損**（鎖定一、二輪結果）
| 參數 | 範圍 | 步長 | 組合數 |
|------|------|------|--------|
| `TrailActivate` | 0 ~ 100 | 10 | 11 |
| `TrailOffset` | 10 ~ 50 | 10 | 5 |
| **小計** | | | **55** |

**第四輪：時間參數**（鎖定一~三輪結果）
| 參數 | 範圍 | 步長 | 組合數 |
|------|------|------|--------|
| `ExitTime` | 0400 ~ 0500 | 30 | 3 |
| **小計** | | | **3** |

> **分階段總計**：1,584 + 200 + 55 + 3 = **1,842 組合**（暴力掃描幾分鐘內完成）
> 缺點：分階段可能錯過跨組參數間的交互作用。GA 全掃較能捕捉交互效果。

### 六、MC12 優化後的觀察重點

1. **確認高原**：最佳參數附近 ±2 步長的績效是否穩定（非尖峰）
2. **比對 Python 結果**：WF 收斂到 LB=6/EO=6/SL=80/TP=160，MC12 是否類似
3. **追蹤停損效果**：日線代理無法驗證，這是 MC12 最重要的新發現
4. **15M vs 30M**：哪個週期的 MDD 更低、勝率更穩定
5. **實際 MDD**：是否低於 90,000 NTD（300k 帳戶 30%）
6. **GA 收斂穩定性**：跑 2~3 次 GA（不同 seed），看最佳參數是否收斂到同一區域
